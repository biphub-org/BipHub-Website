---
phase: 08-edit-approved-request-changes
plan: 04
type: execute
wave: 2
depends_on: [08-02]
files_modified:
  - lib/schemas/bip-edits.ts
  - lib/queries/bipEdits.ts
  - lib/queries/coordinatorBipById.ts
  - lib/queries/adminBips.ts
autonomous: true
requirements: [EDIT-01, EDIT-03, EDIT-06]
user_setup: []

must_haves:
  truths:
    - "Zod v3 schemas exist for approve/reject/request-changes (edit) and request-changes (new submission), all with min 10 / max 1000 notes"
    - "getOpenEditForBip and getBipEditById return the proposed content + admin_note for the diff and form pre-fill"
    - "getAdminPendingSubmissions returns bips in ('pending','changes_requested'); getAdminPendingEdits returns pending/changes_requested bip_edits joined to parent bips"
    - "getCoordinatorBipById accepts approved + changes_requested and returns an openEdit sub-object"
    - "getLatestChangesRequest returns the bip_status_history note for a new-submission changes_requested"
  artifacts:
    - path: "lib/schemas/bip-edits.ts"
      provides: "ApproveEditSchema, RejectEditSchema, RequestChangesEditSchema, RequestChangesBipSchema"
      contains: "RequestChangesEditSchema"
    - path: "lib/queries/bipEdits.ts"
      provides: "edit + admin-queue + latest-changes-request queries"
      contains: "getAdminPendingEdits"
    - path: "lib/queries/coordinatorBipById.ts"
      provides: "extended whitelist + openEdit"
      contains: "openEdit"
  key_links:
    - from: "getCoordinatorBipById"
      to: "bip_edits open row"
      via: "openEdit sub-object with proposed BipDraftData"
      pattern: "openEdit"
    - from: "getAdminPendingEdits"
      to: "bip_edits + bips"
      via: "status in pending/changes_requested + parent join"
      pattern: "bip_edits"
---

<objective>
Wave 2 (parallel with 08-03): the validation + data-access layer. Provide the Zod schemas the Server Actions and modals revalidate against, the queries the admin queue + diff view + coordinator edit form read from, and the extension that lets the coordinator edit form load an approved BIP and its open edit.

Purpose: Implement the read side of D-02/D-03/D-06a/D-07/D-08 so Wave 3 actions and Wave 4 UI have typed contracts to build against (interface-first).
Output: lib/schemas/bip-edits.ts + lib/queries/bipEdits.ts + extended coordinatorBipById.ts + adminBips.ts.
</objective>

<execution_context>
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-edit-approved-request-changes/08-PATTERNS.md
@.planning/phases/08-edit-approved-request-changes/08-RESEARCH.md

<interfaces>
ADMIN_BIP_SELECT + AdminBip live in lib/queries/adminBips.ts (line 97+). getAdminPendingBips (line 112) filters .eq('status','pending'). getNextPendingBip (line 217). getAdminBipById (line 144) returns BipDetail.
getCoordinatorBipById (lib/queries/coordinatorBipById.ts ~line 71) currently: `if (data.status !== 'draft' && data.status !== 'pending') return null`.
BipDraftData / FullBipValues shape = lib/schemas/bip-wizard.ts fullBipSchema (the 22 editable fields + partner_universities[]).
bip_status_history has a `note` column (used for rejection reason) and `action_kind`.
auth: getClaims() only (CLAUDE.md). Admin role gate: (claims.app_metadata.role) === 'admin'.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/schemas/bip-edits.ts</name>
  <read_first>
    - lib/schemas/admin-bips.ts (RejectBipSchema pattern: uuid + note min 10 / max 1000)
    - .planning/phases/08-edit-approved-request-changes/08-PATTERNS.md (lib/schemas/bip-edits.ts section — exact schemas)
  </read_first>
  <files>lib/schemas/bip-edits.ts</files>
  <action>
Zod v3 (import { z } from 'zod'). Export (verbatim per 08-PATTERNS.md):
  - ApproveEditSchema = z.object({ editId: z.string().uuid({message:'Invalid edit id.'}) }) + type
  - RejectEditSchema = z.object({ editId: uuid, note: z.string().min(10,'Note must be at least 10 characters.').max(1000,'Note must be at most 1000 characters.') }) + type
  - RequestChangesEditSchema = same shape as RejectEditSchema (editId + note) + type
  - RequestChangesBipSchema = z.object({ bipId: uuid, note: min10/max1000 }) + type   (new-submission path, D-06a)
Header doc comment citing D-03/D-04/D-06a.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "bip-edits.ts" | awk '{ if ($1==0) print "ok"; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Schema = z.object" lib/schemas/bip-edits.ts >= 4
    - grep -q "RequestChangesBipSchema" AND grep -q "RequestChangesEditSchema" lib/schemas/bip-edits.ts
    - grep -q "min(10" AND grep -q "max(1000" lib/schemas/bip-edits.ts
  </acceptance_criteria>
  <done>Four Zod v3 schemas (+ inferred types) for the edit verdict actions and new-submission request-changes.</done>
</task>

<task type="auto">
  <name>Task 2: Create lib/queries/bipEdits.ts (edit + admin-queue + history reads)</name>
  <read_first>
    - lib/queries/adminBips.ts (auth/role guard lines 112-157, ADMIN_BIP_SELECT line 97, getAdminPendingBips, getNextPendingBip)
    - lib/queries/coordinatorBipById.ts (BipDraftData mapping)
    - .planning/phases/08-edit-approved-request-changes/08-RESEARCH.md (§8 union query shape; §"Diff View" field list)
  </read_first>
  <files>lib/queries/bipEdits.ts</files>
  <action>
Create lib/queries/bipEdits.ts using createClient() + getClaims() guards (NEVER getSession). Export:
  - type AdminBipEditItem = { id, bip_id, status, created_at, admin_note: string|null, bip: AdminBip }  (parent join for queue display)
  - type BipEditDetail = { id, bip_id, status, admin_note: string|null, created_by, data: BipDraftData }  (proposed content mapped to the wizard shape for diff + pre-fill)
  - getOpenEditForBip(bipId): coordinator-scoped; select * from bip_edits where bip_id=eq AND status in ('pending','changes_requested') maybeSingle; map to BipEditDetail or null.
  - getBipEditById(editId): admin-scoped (role==='admin' else null); select the bip_edits row by id; map to BipEditDetail.
  - getAdminPendingSubmissions(): admin-scoped; bips .select(ADMIN_BIP_SELECT) .in('status',['pending','changes_requested']) .order created_at asc → AdminBip[]. (Reuse/export ADMIN_BIP_SELECT from adminBips.ts; if not exported, import it after exporting in Task 3.)
  - getAdminPendingEdits(): admin-scoped; bip_edits .in('status',['pending','changes_requested']) with parent bips join → AdminBipEditItem[].
  - getLatestChangesRequest(bipId): select note from bip_status_history where bip_id=eq AND action_kind='request_changes' order created_at desc limit 1 → string|null. (OQ#2 Option A read path.)
Map partner_institutions jsonb → partner_universities[] shape in BipEditDetail.data so the diff view and form share one contract.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "bipEdits.ts" | awk '{ if ($1==0) print "ok"; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "getOpenEditForBip" AND "getBipEditById" AND "getAdminPendingSubmissions" AND "getAdminPendingEdits" AND "getLatestChangesRequest" lib/queries/bipEdits.ts
    - grep -q "getClaims" lib/queries/bipEdits.ts AND grep -c "getSession" lib/queries/bipEdits.ts == 0
    - grep -q "action_kind" lib/queries/bipEdits.ts (request_changes history read)
    - grep -q "in('status', \['pending', 'changes_requested'\])\|\['pending', 'changes_requested'\]" lib/queries/bipEdits.ts
  </acceptance_criteria>
  <done>Six typed query functions using getClaims; admin queue union + edit detail + latest-changes-request note read.</done>
</task>

<task type="auto">
  <name>Task 3: Extend coordinatorBipById.ts whitelist + openEdit, export ADMIN_BIP_SELECT</name>
  <read_first>
    - lib/queries/coordinatorBipById.ts (status whitelist ~line 71; return type ~lines 30-36; BipDraftData mapping)
    - lib/queries/adminBips.ts (ADMIN_BIP_SELECT const ~line 97 — add `export` if Task 2 imports it)
    - lib/queries/bipEdits.ts (getOpenEditForBip from Task 2)
    - .planning/phases/08-edit-approved-request-changes/08-RESEARCH.md (§9 CoordinatorBipForEdit type)
  </read_first>
  <files>lib/queries/coordinatorBipById.ts, lib/queries/adminBips.ts</files>
  <action>
1) lib/queries/adminBips.ts: add `export` to ADMIN_BIP_SELECT (if Task 2 imports it) — no behavior change. Do NOT alter getAdminPendingBips here (the queue page switches to the new union queries in Wave 4).
2) lib/queries/coordinatorBipById.ts:
   - Change the whitelist line to: `if (!['draft','pending','approved','changes_requested'].includes(data.status)) return null` (Pitfall 1).
   - Extend the return type to CoordinatorBipForEdit per RESEARCH §9: add `status` and an optional `openEdit?: { id, status:'pending'|'changes_requested', admin_note:string|null, data: BipDraftData } | null`.
   - When data.status === 'approved' (or changes_requested with an edit row), call getOpenEditForBip(bipId) and populate openEdit (proposed content mapped to BipDraftData); otherwise openEdit = null.
   - Keep `data` = the LIVE bips content (diff reference) unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "coordinatorBipById.ts|adminBips.ts" | head -5; grep -q "openEdit" lib/queries/coordinatorBipById.ts && grep -q "'approved'" lib/queries/coordinatorBipById.ts && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "'approved', 'changes_requested'\|'approved','changes_requested'" lib/queries/coordinatorBipById.ts (whitelist widened)
    - grep -q "openEdit" lib/queries/coordinatorBipById.ts (return type + population)
    - grep -q "export const ADMIN_BIP_SELECT\|export { ADMIN_BIP_SELECT" lib/queries/adminBips.ts (if imported by Task 2)
    - `npx tsc --noEmit` clean for both files
  </acceptance_criteria>
  <done>Coordinator edit query returns approved/changes_requested BIPs with their open edit; ADMIN_BIP_SELECT exported for reuse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| coordinator/admin JWT → query layer | Reads must enforce ownership/role even though RLS also guards |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Information Disclosure | coordinator reading another coordinator's open edit | mitigate | getOpenEditForBip relies on bip_edits_select_own RLS + getClaims; getBipEditById gated role==='admin' |
| T-08-11 | Elevation | non-admin reaching admin queue queries | mitigate | getAdminPendingSubmissions/Edits return null/empty unless role==='admin' |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for all four files
- grep assertions pass
</verification>

<success_criteria>
Typed read + validation contracts ready: edit schemas, admin-queue union queries, edit detail for diff/pre-fill, coordinator approved-BIP load with openEdit, new-submission changes-request note read.
</success_criteria>

<output>
After completion, create `.planning/phases/08-edit-approved-request-changes/08-04-SUMMARY.md`
</output>
