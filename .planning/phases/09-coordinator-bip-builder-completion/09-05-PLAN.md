---
phase: 09-coordinator-bip-builder-completion
plan: 05
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - lib/queries/bipDetail.ts
  - lib/queries/coordinatorBipById.ts
  - components/forms/wizardAdapter.ts
autonomous: true
requirements: [SUBM-09, SUBM-10, SUBM-11, SUBM-14]
must_haves:
  truths:
    - "The BipDetail type and both detail-query select strings carry the four new fields, so the admin diff view can read live values for them"
    - "Re-opening a draft/approved BIP in the edit wizard pre-fills the four new fields from the live bips row (no silent blanking)"
    - "The Step-5 wizard preview adapter compiles against the extended BipDetail type"
  artifacts:
    - path: "lib/queries/bipDetail.ts"
      provides: "BipDetail type + getBipBySlug/getBipById select strings extended with the four new fields"
      contains: "virtual_sessions_count"
    - path: "lib/queries/coordinatorBipById.ts"
      provides: "Coordinator edit-mode select + draft construction carrying the four new fields"
      contains: "accommodation_notes"
    - path: "components/forms/wizardAdapter.ts"
      provides: "draftToBipDetail maps the four new fields onto the preview BipDetail"
      contains: "partner_institutions_only"
  key_links:
    - from: "lib/queries/coordinatorBipById.ts .select() + draft"
      to: "wizard hydrateFromServer"
      via: "new fields flow from live bips row into the edit wizard"
      pattern: "virtual_duration_notes"
    - from: "lib/queries/bipDetail.ts BipDetail type"
      to: "components/admin/BipEditDiffView.tsx getLive (Plan 09-07)"
      via: "diff view reads the four new fields off BipDetail"
      pattern: "accommodation_notes"
---

<objective>
Extend the `BipDetail` type + both detail-query select strings and the two unlisted propagation surfaces RESEARCH discovered (`coordinatorBipById.ts` edit pre-fill and `wizardAdapter.ts` preview) so the four new fields survive read-back and compile against the finalized type.

Purpose: This is the "read/type" contract half of the propagation map. The admin diff view (Plan 09-07) reads live values off `BipDetail`; the coordinator edit wizard pre-fills from `coordinatorBipById.ts`; the Step-5 preview builds a `BipDetail` literal in `wizardAdapter.ts`. Missing any of these produces silent blank pre-fill or a build break — the exact anti-Pitfall-1 failure. NOTE: only the type + query plumbing is in scope; the public detail-page RENDER (BipBody/BipSidebar/BipHeader) is Phase 10 and MUST NOT be touched.
Output: Three files carrying the four new fields.
</objective>

<execution_context>
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-coordinator-bip-builder-completion/09-RESEARCH.md
@lib/queries/bipDetail.ts
@lib/queries/coordinatorBipById.ts
@components/forms/wizardAdapter.ts
@lib/store/bip-draft.ts

<interfaces>
- `lib/queries/bipDetail.ts`: add to the `BipDetail` type (nullable to match style): `virtual_sessions_count: number | null`, `virtual_duration_notes: string | null`, `accommodation_notes: string | null`, `partner_institutions_only: boolean | null`. Add the four column names to BOTH `.select()` template strings (getBipBySlug lines 74-88 and getBipById lines 125-139). Do NOT add `max_participants` (its exclusion from BipDetail is intentional — DETL-15 / Phase 10 territory).
- `lib/queries/coordinatorBipById.ts`: add the four columns to the `.select()` string (lines 62-76) and set them on the constructed `draft: BipDraftData` object (lines 111-167): `virtual_sessions_count: data.virtual_sessions_count ?? undefined`, `virtual_duration_notes: data.virtual_duration_notes ?? undefined`, `accommodation_notes: data.accommodation_notes ?? undefined`, `partner_institutions_only: data.partner_institutions_only ?? false`.
- `components/forms/wizardAdapter.ts`: `draftToBipDetail` builds a `BipDetail` literal (lines 51-110). Because the four fields are now required (nullable) keys on `BipDetail`, add them: `virtual_sessions_count: draft.virtual_sessions_count ?? null`, `virtual_duration_notes: draft.virtual_duration_notes ?? null`, `accommodation_notes: draft.accommodation_notes ?? null`, `partner_institutions_only: draft.partner_institutions_only ?? false`.
</interfaces>

<critical>
- Do NOT modify `components/bip/BipBody.tsx`, `BipSidebar.tsx`, or `BipHeader.tsx` — public detail-page rendering is Phase 10 (out of scope). This plan is type + query plumbing only.
- Depends on Plan 09-01 (regenerated database.types.ts) so the `.select()` on bips new columns type-checks, and Plan 09-02 (BipDraftData carries the four fields).
</critical>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend BipDetail type + both detail select strings</name>
  <read_first>
    - lib/queries/bipDetail.ts (BipDetail type lines 15-60; getBipBySlug select lines 74-88; getBipById select lines 125-139)
  </read_first>
  <action>
    Add the four nullable fields to the `BipDetail` type and append the four column names to BOTH `.select()` template strings (put them alongside the existing scalar columns, e.g. after `subject_areas`). Do not add `max_participants`. Do not touch the public rendering components.
  </action>
  <verify>
    <automated>grep -Eo "virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only" lib/queries/bipDetail.ts | sort -u | wc -l | grep -q 4 && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The four field names each appear at least 3 times in `lib/queries/bipDetail.ts` (type + 2 select strings): `grep -c "accommodation_notes" lib/queries/bipDetail.ts` >= 3.
    - `grep -c "max_participants" lib/queries/bipDetail.ts` returns 0 (intentionally excluded).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>BipDetail carries the four fields in the type and both queries.</done>
</task>

<task type="auto">
  <name>Task 2: Coordinator edit pre-fill carries the four fields</name>
  <read_first>
    - lib/queries/coordinatorBipById.ts (.select() lines 62-76; draft construction lines 111-167)
  </read_first>
  <action>
    Add the four columns to the `.select()` string and set them on the constructed `draft` object per the `<interfaces>` mapping. This ensures re-opening an existing BIP for editing pre-fills the new fields from the live `bips` row rather than showing blanks.
  </action>
  <verify>
    <automated>grep -Eo "virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only" lib/queries/coordinatorBipById.ts | sort -u | wc -l | grep -q 4 && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Each of the four field names appears at least twice in `lib/queries/coordinatorBipById.ts` (select + draft): `grep -c "partner_institutions_only" lib/queries/coordinatorBipById.ts` >= 2.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Edit-mode pre-fill reflects the live values of all four new fields.</done>
</task>

<task type="auto">
  <name>Task 3: Wizard preview adapter maps the four fields</name>
  <read_first>
    - components/forms/wizardAdapter.ts (draftToBipDetail literal lines 51-110)
  </read_first>
  <action>
    Add the four fields to the `BipDetail` literal returned by `draftToBipDetail` per the `<interfaces>` mapping so the Step-5 preview compiles and reflects the entered values.
  </action>
  <verify>
    <automated>grep -Eo "virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only" components/forms/wizardAdapter.ts | sort -u | wc -l | grep -q 4 && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eo "virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only" components/forms/wizardAdapter.ts | sort -u | wc -l` returns 4.
    - `npx tsc --noEmit` exits 0 (confirms the required-key BipDetail literal is complete).
  </acceptance_criteria>
  <done>The preview adapter builds a complete BipDetail with the four new fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → RSC/query layer | Read-only projection of BIP content into typed shapes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Info disclosure | coordinatorBipById select | accept | Query retains its existing `eq('created_by', claims.sub)` owner filter + getClaims; new columns inherit the same row-level RLS |
| T-09-05-02 | Tampering | BipDetail type completeness | mitigate | Fields added as required (nullable) keys so tsc enforces every producer (adapter + both selects) supplies them — prevents a silently-partial shape |
</threat_model>

<verification>
- `grep -Eo "virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only" lib/queries/bipDetail.ts lib/queries/coordinatorBipById.ts components/forms/wizardAdapter.ts | sort -u | wc -l` == 4
- `grep -c "max_participants" lib/queries/bipDetail.ts` == 0
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
The four new fields are present on the BipDetail type, both detail queries, the coordinator edit pre-fill, and the wizard preview adapter — with public rendering untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/09-coordinator-bip-builder-completion/09-05-SUMMARY.md`.
</output>
