---
phase: 09-coordinator-bip-builder-completion
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - components/forms/steps/WizardStep2ProgramDetails.tsx
  - components/forms/steps/WizardStep3Partners.tsx
  - components/forms/steps/WizardStep4ApplicationInfo.tsx
autonomous: true
requirements: [SUBM-09, SUBM-10, SUBM-11, SUBM-12, SUBM-13]
must_haves:
  truths:
    - "A coordinator can enter virtual-session count and duration/schedule notes in the virtual-component step (Step 2)"
    - "The Step-2 virtual-timing selector offers exactly the 5 DB-valid options (before/during/after/before_and_after/mixed) — 'concurrent' is gone"
    - "The Step-2 participant field enforces a minimum of 10"
    - "A coordinator can mark a BIP open only to partner institutions in the Partners step (Step 3)"
    - "A coordinator can add accommodation notes in the application/practical step (Step 4)"
  artifacts:
    - path: "components/forms/steps/WizardStep2ProgramDetails.tsx"
      provides: "virtual_sessions_count number input + virtual_duration_notes textarea; 5-option virtual_timing select; max_participants min=10"
      contains: "virtual_sessions_count"
    - path: "components/forms/steps/WizardStep3Partners.tsx"
      provides: "partner_institutions_only checkbox mirrored into the draft via mergeDraft (non-RHF pattern)"
      contains: "partner_institutions_only"
    - path: "components/forms/steps/WizardStep4ApplicationInfo.tsx"
      provides: "accommodation_notes textarea following the eligibility_notes RHF pattern"
      contains: "accommodation_notes"
  key_links:
    - from: "WizardStep3Partners.tsx checkbox onChange"
      to: "bip-draft store"
      via: "mergeDraft({ partner_institutions_only })"
      pattern: "mergeDraft"
    - from: "WizardStep2 virtual_timing <select>"
      to: "DB CHECK set"
      via: "option values before/during/after/before_and_after/mixed"
      pattern: "before_and_after"
---

<objective>
Wire the four new fields (and the corrected virtual_timing / participant floor) into the wizard UI, placed per the locked decisions: virtual session detail in Step 2 (D-02), partner-only flag in Step 3 (D-01), accommodation notes in Step 4 (D-03).

Purpose: Plan 09-02 defined the schema + draft contract; this plan gives coordinators the actual inputs. Note the framework split — Steps 2 and 4 use react-hook-form; Step 3 does NOT (raw form + manual state mirrored into the draft).
Output: Three updated wizard step components.
</objective>

<execution_context>
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-coordinator-bip-builder-completion/09-CONTEXT.md
@components/forms/steps/WizardStep2ProgramDetails.tsx
@components/forms/steps/WizardStep3Partners.tsx
@components/forms/steps/WizardStep4ApplicationInfo.tsx
@lib/schemas/bip-wizard.ts

<interfaces>
- Step 2 (RHF): the `virtual_timing` `<select>` currently lists before/after/concurrent (lines 129-131). Replace with 5 options: before → "Before physical mobility", during → "During physical mobility", after → "After physical mobility", before_and_after → "Before and after physical mobility", mixed → "Mixed / throughout". The `max_participants` `<Input min={5} max={20}>` (line 218) → `min={10}` and update the FormDescription ("5–20" → "10–20 participants"). Add `virtual_sessions_count` (`<Input type="number" min={0}>`) and `virtual_duration_notes` (`<Textarea rows={2}>`) as new FormField blocks near the virtual_component_description field. Extend the `useForm` defaultValues + the mergeDraft watch payload so RHF tracks the new fields.
- Step 3 (NON-RHF): raw `<form>` with `useState`. Add `const [partnerOnly, setPartnerOnly] = useState<boolean>(draft.partner_institutions_only ?? false)`. On change, call BOTH `setPartnerOnly(next)` and `mergeDraft({ partner_institutions_only: next })` — mirror the exact `commit()` pattern already used for `partners`. Render a `<Checkbox>` (already imported elsewhere; import from `@/components/ui/checkbox`) with a label like "Open only to partner-institution students". Do NOT wrap it in `<FormField>` — there is no `<Form>` in this file.
- Step 4 (RHF): add an `accommodation_notes` `<Textarea>` FormField modeled exactly on the existing `eligibility_notes` field (lines 117-133), including default value `draft.accommodation_notes ?? ''`.
</interfaces>

<critical>
- Step 3 is NOT a react-hook-form component (RESEARCH §Wizard UI Framework Split). Using `<FormField>` there will break — use `useState` + `mergeDraft`.
- No dynamic Tailwind class names (CLAUDE.md). Reuse existing input/textarea/checkbox primitives.
</critical>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step 2 — virtual session detail, 5-option timing, participant floor</name>
  <read_first>
    - components/forms/steps/WizardStep2ProgramDetails.tsx (virtual_timing select lines 116-137; max_participants field lines 211-224; useForm defaultValues lines 64-76)
    - lib/schemas/bip-wizard.ts (the Step2Values type now carrying the new fields)
  </read_first>
  <action>
    In `WizardStep2ProgramDetails.tsx`:
    - Rebuild the `virtual_timing` `<select>` options to the 5 DB-valid values with the labels in the `<interfaces>` block.
    - Change `max_participants` `<Input min={5} max={20}>` to `min={10} max={20}` and update its FormDescription copy to "Erasmus+ KA131 BIPs allow 10–20 participants."
    - Add two new FormField blocks: `virtual_sessions_count` (`<Input type="number" min={0} placeholder="e.g. 4" />`) and `virtual_duration_notes` (`<Textarea rows={2} placeholder="e.g. 4 weekly 90-min sessions before mobility" />`), positioned with the other virtual-component fields.
    - Extend `useForm` `defaultValues` with `virtual_sessions_count: draft.virtual_sessions_count ?? undefined` and `virtual_duration_notes: draft.virtual_duration_notes ?? ''`. The existing `form.watch` effect already funnels all fields into `mergeDraft`/`onAutoSave`, so no watch change is needed beyond registering the fields.
  </action>
  <verify>
    <automated>grep -q "virtual_sessions_count" components/forms/steps/WizardStep2ProgramDetails.tsx && grep -q "before_and_after" components/forms/steps/WizardStep2ProgramDetails.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eo "before_and_after|value=\"mixed\"|value=\"during\"" components/forms/steps/WizardStep2ProgramDetails.tsx | sort -u | wc -l` returns 3 (all new timing options present).
    - `grep -vE '^\s*(//|\*)' components/forms/steps/WizardStep2ProgramDetails.tsx | grep -c "concurrent"` returns 0.
    - `grep -c "min={10}" components/forms/steps/WizardStep2ProgramDetails.tsx` >= 1; `grep -c "min={5}" components/forms/steps/WizardStep2ProgramDetails.tsx` returns 0.
    - `grep -Eo "virtual_sessions_count|virtual_duration_notes" components/forms/steps/WizardStep2ProgramDetails.tsx | sort -u | wc -l` returns 2.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Step 2 offers the correct timing options, enforces the 10 floor in the UI, and captures virtual session count + duration notes.</done>
</task>

<task type="auto">
  <name>Task 2: Step 3 — partner-only checkbox (non-RHF)</name>
  <read_first>
    - components/forms/steps/WizardStep3Partners.tsx (useState + commit() mirroring pattern lines 44-55; the raw form has no `<Form>` wrapper)
  </read_first>
  <action>
    In `WizardStep3Partners.tsx`, add `const [partnerOnly, setPartnerOnly] = useState<boolean>(draft.partner_institutions_only ?? false)`. Render a `<Checkbox>` (import `Checkbox` from `@/components/ui/checkbox`) with an associated label "Open only to partner-institution students" and a short helper line. On `onCheckedChange={(next) => { setPartnerOnly(Boolean(next)); mergeDraft({ partner_institutions_only: Boolean(next) }) }}`. Place it below the partner list (near the "Selected partners" block) so it reads with the partner context. Do NOT use `<FormField>`.
  </action>
  <verify>
    <automated>grep -q "partner_institutions_only" components/forms/steps/WizardStep3Partners.tsx && grep -q "mergeDraft" components/forms/steps/WizardStep3Partners.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "partner_institutions_only" components/forms/steps/WizardStep3Partners.tsx` >= 1.
    - `grep -c "mergeDraft({ partner_institutions_only" components/forms/steps/WizardStep3Partners.tsx` >= 1.
    - `grep -c "FormField" components/forms/steps/WizardStep3Partners.tsx` returns 0 (non-RHF discipline preserved).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The partner-only checkbox in Step 3 mirrors its value into the draft store on every change.</done>
</task>

<task type="auto">
  <name>Task 3: Step 4 — accommodation notes textarea</name>
  <read_first>
    - components/forms/steps/WizardStep4ApplicationInfo.tsx (eligibility_notes FormField lines 117-133; useForm defaultValues lines 46-54)
  </read_first>
  <action>
    In `WizardStep4ApplicationInfo.tsx`, add an `accommodation_notes` `<Textarea>` FormField modeled on `eligibility_notes` (label "Accommodation notes (optional)", helper "Housing, cost, or booking guidance for participants — leave blank if not offered."). Add `accommodation_notes: draft.accommodation_notes ?? ''` to `useForm` defaultValues. The existing `form.watch` effect propagates it to the draft automatically.
  </action>
  <verify>
    <automated>grep -q "accommodation_notes" components/forms/steps/WizardStep4ApplicationInfo.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "accommodation_notes" components/forms/steps/WizardStep4ApplicationInfo.tsx` >= 2 (FormField name + defaultValues).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Step 4 captures accommodation notes into the draft.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coordinator input → draft store | Client-side capture; server re-validates at submit (Plan 09-02/09-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Tampering | client-side wizard validation | accept | Client validation is UX only; the trust boundary is the server-side fullBipSchema re-validation in the submit/edit actions (Plans 09-02/09-06) |
| T-09-04-02 | Info disclosure | free-text notes | accept | Plain-text, length-bounded via Zod, rendered as React text content only (no rich text) |
</threat_model>

<verification>
- `grep -vE '^\s*(//|\*)' components/forms/steps/WizardStep2ProgramDetails.tsx | grep -c "concurrent"` == 0
- `grep -Eo "virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only" components/forms/steps/WizardStep2ProgramDetails.tsx components/forms/steps/WizardStep3Partners.tsx components/forms/steps/WizardStep4ApplicationInfo.tsx | sort -u | wc -l` == 4
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
All four new fields are enterable in the correct wizard steps, the timing selector matches the DB, and the participant floor is 10 in the UI.
</success_criteria>

<output>
After completion, create `.planning/phases/09-coordinator-bip-builder-completion/09-04-SUMMARY.md`.
</output>
