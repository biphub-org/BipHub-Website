---
phase: 09-coordinator-bip-builder-completion
plan: 08
type: execute
wave: 4
depends_on: [01, 07]
files_modified:
  - supabase/seed.sql
  - supabase/seed.e2e.sql
  - scripts/seed-cloud-e2e.mjs
  - scripts/verify-seed.ts
autonomous: true
requirements: [SUBM-09, SUBM-10, SUBM-11, FOUN-14]
must_haves:
  truths:
    - "All three seed sources exercise the four new fields with at least one non-default value across the fixture set"
    - "The e2e edit-target fixture (e2e-edit-target-bip) carries non-default values for all four new fields so the per-field round-trip specs have something to change"
    - "verify-seed asserts a distribution for at least one new field and passes"
  artifacts:
    - path: "supabase/seed.sql"
      provides: "Local seed with virtual_sessions_count/virtual_duration_notes populated and a spread of partner_institutions_only/accommodation_notes values"
      contains: "virtual_sessions_count"
    - path: "supabase/seed.e2e.sql"
      provides: "E2E seed carrying the four new fields (incl. the e2e-edit-target-bip fixture)"
      contains: "partner_institutions_only"
    - path: "scripts/seed-cloud-e2e.mjs"
      provides: "Cloud e2e seed in lockstep with seed.e2e.sql for the four new fields"
      contains: "accommodation_notes"
    - path: "scripts/verify-seed.ts"
      provides: "Distribution check for at least one new field"
      contains: "partner_institutions_only"
  key_links:
    - from: "supabase/seed.e2e.sql / scripts/seed-cloud-e2e.mjs"
      to: "tests/e2e round-trip specs (Plan 09-09)"
      via: "e2e-edit-target-bip seeded with non-default new-field values"
      pattern: "e2e-edit-target-bip"
---

<objective>
Update all three seed sources for the four new fields (FOUN-14) and extend `verify-seed.ts` with a distribution check, keeping `seed.e2e.sql` and `scripts/seed-cloud-e2e.mjs` in lockstep. Seed the `e2e-edit-target-bip` fixture with non-default values so Plan 09-09's per-field edit→approve→persist specs have real data to change.

Purpose: Seed drift already caused BUG-002 (MEMORY: the two e2e seed files must stay in sync). A field wired into code but absent from the fixtures cannot be proven by E2E, and the round-trip specs need a starting value to edit.
Output: Three updated seed sources, an extended verify script, and a re-seeded cloud TEST project.
</objective>

<execution_context>
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-coordinator-bip-builder-completion/09-RESEARCH.md
@supabase/seed.sql
@supabase/seed.e2e.sql
@scripts/seed-cloud-e2e.mjs
@scripts/verify-seed.ts

<interfaces>
- `supabase/seed.sql`: the 20-BIP INSERT already lists `accommodation_notes` and `partner_institutions_only` columns (BIP 19/20 flagged partner-only) but NOT `virtual_sessions_count` / `virtual_duration_notes`. Add those two columns to the INSERT column list and provide values on a subset of rows (e.g. virtual_sessions_count on ~6 rows, virtual_duration_notes on those same rows). Ensure at least a few rows set partner_institutions_only=true and a few set accommodation_notes.
- `supabase/seed.e2e.sql` + `scripts/seed-cloud-e2e.mjs`: these are kept in lockstep. Add the four new fields to the fixture INSERTs. CRITICAL: the edit-target fixture (UUID e2e0bbbb-bbbb-bbbb-bbbb-000000000010, slug e2e-edit-target-bip) MUST carry non-default values for ALL FOUR new fields (e.g. virtual_sessions_count=4, virtual_duration_notes='Weekly online seminars', accommodation_notes='Dorm rooms reserved', partner_institutions_only=true) so the round-trip specs can flip them.
- `scripts/verify-seed.ts`: the select at line 51 lists columns for the audit; add at least `partner_institutions_only` to that select and a `check('partner_only_ge_1', ...)` assertion asserting >= 1 seeded BIP has partner_institutions_only=true, mirroring the existing green_travel/inclusion_support distribution checks (lines 147-152).
</interfaces>

<critical>
- MEMORY / E2E seed sync: `seed.e2e.sql` and `scripts/seed-cloud-e2e.mjs` MUST carry identical new-field values for the same fixtures — a fresh cloud re-seed that drifts will break Plan 09-09's specs and can nuke @biphub.test users.
- Pitfall 4 / cloud target: before re-seeding the cloud TEST project (`zbvcpiwbopmfbjfhzprw`), confirm migration 00022 is applied there (else the seed 400s on the missing columns). Apply the migration to that project first if needed.
- All seeded values must satisfy the corrected rules: virtual_timing ∈ the 5 DB values, max_participants ≥ 10 (audit confirms current minimum is 15/16 — nothing to fix, but do not introduce sub-10 values).
</critical>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate the four new fields in local seed.sql</name>
  <read_first>
    - supabase/seed.sql (bips INSERT column list lines 77-110; confirm accommodation_notes/partner_institutions_only already present, virtual_sessions_count/virtual_duration_notes absent)
  </read_first>
  <action>
    Add `virtual_sessions_count` and `virtual_duration_notes` to the `insert into public.bips (...)` column list and provide values on a representative subset of the 20 rows. Ensure the fixture set exercises all four new fields with at least one non-default value each (partner_institutions_only=true on a few rows, accommodation_notes populated on a few rows). Keep every virtual_timing value within the 5 DB-valid options and max_participants ≥ 10.
  </action>
  <verify>
    <automated>grep -q "virtual_sessions_count" supabase/seed.sql && grep -q "virtual_duration_notes" supabase/seed.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eo "virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only" supabase/seed.sql | sort -u | wc -l` returns 4.
    - No sub-10 participant values introduced and no 'concurrent' timing: `grep -c "concurrent" supabase/seed.sql` returns 0.
  </acceptance_criteria>
  <done>seed.sql exercises all four new fields.</done>
</task>

<task type="auto">
  <name>Task 2: Update the two e2e seed sources in lockstep (incl. edit-target fixture)</name>
  <read_first>
    - supabase/seed.e2e.sql (edit-target fixture lines ~446-522)
    - scripts/seed-cloud-e2e.mjs (fixture objects lines ~144-293)
  </read_first>
  <action>
    Add the four new fields to both e2e seed sources for the same fixtures, with IDENTICAL values. Set the `e2e-edit-target-bip` fixture (e2e0bbbb-bbbb-bbbb-bbbb-000000000010) with non-default values for all four fields per the `<interfaces>` block, so Plan 09-09 can edit each field and assert persistence.
  </action>
  <verify>
    <automated>grep -q "virtual_sessions_count" supabase/seed.e2e.sql && grep -q "virtual_sessions_count" scripts/seed-cloud-e2e.mjs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both files reference all four fields: `grep -Eo "virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only" supabase/seed.e2e.sql | sort -u | wc -l` returns 4 AND the same for `scripts/seed-cloud-e2e.mjs`.
    - The edit-target fixture carries the new fields (values appear within its INSERT block in both files).
    - No `concurrent` timing values: `grep -c "concurrent" supabase/seed.e2e.sql scripts/seed-cloud-e2e.mjs` returns 0.
  </acceptance_criteria>
  <done>Both e2e seed sources carry the four new fields in lockstep, with the edit-target fixture pre-populated.</done>
</task>

<task type="auto">
  <name>Task 3: Extend verify-seed and run it green (local)</name>
  <read_first>
    - scripts/verify-seed.ts (select line 51; green_travel/inclusion_support distribution checks lines 147-152)
  </read_first>
  <action>
    Add `partner_institutions_only` to the audit select and a `check(...)` asserting at least one seeded BIP has it true (mirror the green_travel distribution check). Run `npm run verify:seed` and ensure it passes.
  </action>
  <verify>
    <automated>grep -q "partner_institutions_only" scripts/verify-seed.ts && npm run verify:seed && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-seed.ts` selects and asserts on `partner_institutions_only`: `grep -c "partner_institutions_only" scripts/verify-seed.ts` >= 2 (select + check).
    - `npm run verify:seed` exits 0 (all checks pass, including the new distribution check).
  </acceptance_criteria>
  <done>verify-seed passes with a new-field distribution assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script → cloud TEST DB | Service-role seed writes to the dedicated test project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08-01 | Tampering | cloud e2e re-seed | mitigate | Re-seed only targets the guarded TEST ref (zbvcpiwbopmfbjfhzprw); seed values kept in lockstep across both e2e sources to avoid the BUG-002 drift class |
| T-09-08-02 | Denial of Service | destructive re-seed nuking test users | accept | Documented in MEMORY; the edit-target fixture and @biphub.test users are re-created by the seed itself; no production data touched |
</threat_model>

<verification>
- `grep -Eo "virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only" supabase/seed.sql supabase/seed.e2e.sql scripts/seed-cloud-e2e.mjs | sort -u | wc -l` == 4
- `npm run verify:seed` exits 0
- `grep -c "concurrent" supabase/seed.sql supabase/seed.e2e.sql scripts/seed-cloud-e2e.mjs` == 0
</verification>

<success_criteria>
All three seed sources exercise the four new fields (edit-target fixture pre-populated), verify-seed passes, and the cloud TEST project is re-seeded ready for E2E.
</success_criteria>

<output>
After completion, create `.planning/phases/09-coordinator-bip-builder-completion/09-08-SUMMARY.md`.
</output>
