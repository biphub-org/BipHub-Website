---
phase: 09-coordinator-bip-builder-completion
plan: 06
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - lib/actions/admin-bips.ts
  - lib/actions/bip-edits.ts
autonomous: true
requirements: [SUBM-09, SUBM-10, SUBM-11, SUBM-14]
must_haves:
  truths:
    - "An admin edit of a BIP persists the four new fields to the bips row"
    - "A coordinator edit of an approved BIP writes the four new fields into the bip_edits shadow row"
  artifacts:
    - path: "lib/actions/admin-bips.ts"
      provides: "adminUpdateBipAction updatePayload carries the four new fields"
      contains: "virtual_sessions_count"
    - path: "lib/actions/bip-edits.ts"
      provides: "buildContentPayload param type + return object carry the four new fields (feeds submitEditAction/resubmitEditAction/resubmitPendingBipAction)"
      contains: "accommodation_notes"
  key_links:
    - from: "lib/actions/bip-edits.ts buildContentPayload"
      to: "bip_edits INSERT/UPDATE (submitEditAction/resubmitEditAction)"
      via: "the four new keys in the returned content payload"
      pattern: "partner_institutions_only"
    - from: "lib/actions/admin-bips.ts updatePayload"
      to: "bips UPDATE"
      via: "the four new keys written on admin edit"
      pattern: "virtual_duration_notes"
---

<objective>
Add the four new fields to the two write paths that feed off `fullBipSchema`: the admin direct-edit path (`adminUpdateBipAction` → `bips`) and the coordinator edit-content builder (`buildContentPayload` → `bip_edits`, used by all three edit actions).

Purpose: These are the write halves of the propagation map. Validation is already automatic (both call `fullBipSchema.safeParse`, corrected in Plan 09-02); this plan carries the validated values into the DB payloads. `buildContentPayload` is the single helper feeding `submitEditAction`, `resubmitEditAction`, and `resubmitPendingBipAction`, so one change covers all three.
Output: Two updated Server Action files.
</objective>

<execution_context>
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-coordinator-bip-builder-completion/09-RESEARCH.md
@lib/actions/admin-bips.ts
@lib/actions/bip-edits.ts
@lib/schemas/bip-wizard.ts

<interfaces>
- `lib/actions/admin-bips.ts` — `adminUpdateBipAction` `updatePayload` (lines 357-387): add `virtual_sessions_count: parsed.data.virtual_sessions_count ?? null`, `virtual_duration_notes: parsed.data.virtual_duration_notes || null`, `accommodation_notes: parsed.data.accommodation_notes || null`, `partner_institutions_only: parsed.data.partner_institutions_only ?? false`. Do not touch status/slug omissions.
- `lib/actions/bip-edits.ts` — `buildContentPayload` (lines 32-89): extend the param type with the four fields (`virtual_sessions_count?: number`, `virtual_duration_notes?: string`, `accommodation_notes?: string`, `partner_institutions_only?: boolean`) and add them to the returned object: `virtual_sessions_count: data.virtual_sessions_count ?? null`, `virtual_duration_notes: data.virtual_duration_notes || null`, `accommodation_notes: data.accommodation_notes || null`, `partner_institutions_only: data.partner_institutions_only ?? false`. Keep slug excluded. These keys write into `bip_edits`, whose columns now exist (Plan 09-01).
</interfaces>

<critical>
- `buildContentPayload` writes to `bip_edits` — depends on Plan 09-01's regenerated types so the four new keys type-check against the bip_edits Insert type. If tsc errors on unknown columns, Plan 09-01's push/regen did not complete.
- Do NOT alter the intentional slug/status exclusions or the `subject_area`/`isced_f_code` legacy mirroring already in these payloads.
</critical>
</context>

<tasks>

<task type="auto">
  <name>Task 1: admin edit path writes the four fields to bips</name>
  <read_first>
    - lib/actions/admin-bips.ts (adminUpdateBipAction updatePayload lines 357-387)
  </read_first>
  <action>
    Add the four keys to `updatePayload` per the `<interfaces>` mapping. Leave the `status`/`slug` omissions and the partner delete-then-insert reconciliation unchanged.
  </action>
  <verify>
    <automated>grep -Eo "virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only" lib/actions/admin-bips.ts | sort -u | wc -l | grep -q 4 && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eo "virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only" lib/actions/admin-bips.ts | sort -u | wc -l` returns 4.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>adminUpdateBipAction persists all four new fields.</done>
</task>

<task type="auto">
  <name>Task 2: coordinator edit-content builder carries the four fields</name>
  <read_first>
    - lib/actions/bip-edits.ts (buildContentPayload param type + return object lines 32-89; callers submitEditAction/resubmitEditAction/resubmitPendingBipAction)
  </read_first>
  <action>
    Extend `buildContentPayload`'s parameter type and returned object with the four fields per the `<interfaces>` mapping. Because all three edit actions spread `...buildContentPayload(parsed.data)` into their `bip_edits`/`bips` writes, this single change propagates the fields through the create-edit, resubmit-edit, and resubmit-pending paths.
  </action>
  <verify>
    <automated>grep -Eo "virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only" lib/actions/bip-edits.ts | sort -u | wc -l | grep -q 4 && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Each of the four field names appears at least twice in `lib/actions/bip-edits.ts` (param type + return object): `grep -c "accommodation_notes" lib/actions/bip-edits.ts` >= 2.
    - `npx tsc --noEmit` exits 0 (confirms the bip_edits Insert type accepts the new columns — Plan 09-01 landed).
  </acceptance_criteria>
  <done>buildContentPayload carries the four fields into every coordinator edit write.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validated draft → bips / bip_edits write | Server Action write after fullBipSchema re-validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | Elevation of privilege | admin/coordinator write payloads | accept | Existing getClaims role guards + RLS (bip_edits created_by/status; bips owner/admin) are unchanged; new columns are content fields inheriting row-level policy |
| T-09-06-02 | Tampering | payload field values | mitigate | All four values come from `parsed.data` (post fullBipSchema.safeParse), never from raw request body |
</threat_model>

<verification>
- `grep -Eo "virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only" lib/actions/admin-bips.ts | sort -u | wc -l` == 4
- `grep -Eo "virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only" lib/actions/bip-edits.ts | sort -u | wc -l` == 4
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
Both the admin direct-edit and coordinator shadow-edit write paths persist all four new fields.
</success_criteria>

<output>
After completion, create `.planning/phases/09-coordinator-bip-builder-completion/09-06-SUMMARY.md`.
</output>
