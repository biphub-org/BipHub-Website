---
phase: 09-coordinator-bip-builder-completion
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/schemas/bip-wizard.test.ts
  - lib/schemas/bip-wizard.ts
  - lib/store/bip-draft.ts
  - lib/actions/bip-submit.ts
autonomous: true
requirements: [SUBM-09, SUBM-10, SUBM-11, SUBM-12, SUBM-13, SUBM-14]
must_haves:
  truths:
    - "Every virtual_timing option the builder can offer (before/during/after/before_and_after/mixed) parses; the legacy 'concurrent' value is rejected"
    - "The wizard/Zod participant floor is 10 on every schema path (step2Schema and fullBipSchema) with no path allowing a save below 10"
    - "The four new fields (virtual_sessions_count, virtual_duration_notes, accommodation_notes, partner_institutions_only) are validatable in the wizard schema and carried on the draft store"
    - "The create/submit path validates with the SAME schema as the edit path — no second hand-copied schema can drift"
  artifacts:
    - path: "tests/schemas/bip-wizard.test.ts"
      provides: "Unit coverage for the virtual_timing enum, the participant floor, and the four new field validators on both step2Schema and fullBipSchema"
      min_lines: 40
    - path: "lib/schemas/bip-wizard.ts"
      provides: "Corrected VIRTUAL_TIMINGS (5 values), max_participants floor 10, four new field validators on step2/step3/step4/fullBipSchema"
      contains: "before_and_after"
    - path: "lib/store/bip-draft.ts"
      provides: "BipDraftData extended with the four new fields and the 5-value virtual_timing union"
      contains: "virtual_sessions_count"
    - path: "lib/actions/bip-submit.ts"
      provides: "Create path consolidated onto fullBipSchema (inline submitSchema removed) with the four new fields written to the bips updatePayload"
      contains: "fullBipSchema.safeParse"
  key_links:
    - from: "lib/actions/bip-submit.ts"
      to: "lib/schemas/bip-wizard.ts fullBipSchema"
      via: "import + safeParse (replaces the hand-copied submitSchema — Pitfall 0)"
      pattern: "fullBipSchema"
    - from: "lib/schemas/bip-wizard.ts VIRTUAL_TIMINGS"
      to: "supabase/migrations/00003_bips_full_schema.sql virtual_timing CHECK"
      via: "enum values matched to the DB CHECK set"
      pattern: "before_and_after|mixed"
---

<objective>
Fix the two live data-integrity bugs (SUBM-12 virtual_timing enum mismatch, SUBM-13 participant floor) AND dissolve Pitfall 0 by consolidating the create path onto the single shared `fullBipSchema`. In the same pass, add the four new field validators to the wizard schema and the draft store so downstream plans (wizard UI, write paths, adapters) have the contract.

Purpose: Today the wizard offers `virtual_timing = 'concurrent'`, which is not a valid DB value — it silently fails the CHECK on save. The participant floor is 5 vs the Erasmus+ minimum of 10. And `lib/actions/bip-submit.ts` maintains its own hand-copied `submitSchema` twin of `fullBipSchema` — fixing one leaves the create path broken while the edit path looks fixed. Consolidating removes that entire class of drift.
Output: A new unit test file, the corrected + extended schema, the extended draft store, and a consolidated create path.
</objective>

<execution_context>
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-coordinator-bip-builder-completion/09-RESEARCH.md
@.planning/phases/09-coordinator-bip-builder-completion/09-VALIDATION.md
@lib/schemas/bip-wizard.ts
@lib/store/bip-draft.ts
@lib/actions/bip-submit.ts
@tests/schemas/admin-bips.test.ts

<interfaces>
DB virtual_timing CHECK (00003 line 15) = `('before','during','after','before_and_after','mixed')`. The canonical correct union already exists as `VirtualTiming` in `lib/types/bip.ts` line 33-34 — reuse those 5 values.

Current bug locations:
- `lib/schemas/bip-wizard.ts` line 40: `const VIRTUAL_TIMINGS = ['before','after','concurrent'] as const` — WRONG.
- `lib/schemas/bip-wizard.ts` line 67 (step2Schema) + line 167 (fullBipSchema): `max_participants ... .min(5)` — must be `.min(10)`.
- `lib/store/bip-draft.ts` line 39: `virtual_timing?: 'before'|'after'|'concurrent'` — WRONG union.
- `lib/actions/bip-submit.ts` lines 57-112: inline `submitSchema` (a hand-copied twin of fullBipSchema, incl. line 66 `'concurrent'` and line 72 `.min(5)`). CONSOLIDATE: delete it and use `fullBipSchema`.

New field validators (all OPTIONAL — a BIP may omit them):
- step2Schema + fullBipSchema: `virtual_sessions_count: z.coerce.number().int().min(0).max(50).optional()`, `virtual_duration_notes: z.string().trim().max(500).optional().or(z.literal(''))`
- step3Schema + fullBipSchema: `partner_institutions_only: z.boolean().optional().default(false)`
- step4Schema + fullBipSchema: `accommodation_notes: z.string().trim().max(1000).optional().or(z.literal(''))`

fullBipSchema max_participants max is currently `.max(20)` — leave the upper bound as-is; only raise the floor to 10.
</interfaces>

<critical>
- Pitfall 0 resolution (RESEARCH §Pitfall 0, planning input #2): CONSOLIDATE `submitSchema` → `fullBipSchema`. `fullBipSchema` is a plain exported const (not a server action), so importing it into a `'use server'` file is safe (RESEARCH Assumption A2). This is the anti-drift choice aligned with FOUN-14.
- The DB CHECK on `max_participants` is intentionally NOT changed (RESEARCH OQ1); SUBM-13's binding proof is the Zod floor here + the E2E in Plan 09-09.
- New fields are OPTIONAL. Do not make any of them required — existing seeded/pending BIPs and BIPs without accommodation must still validate.
</critical>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write the wizard-schema unit tests</name>
  <read_first>
    - tests/schemas/admin-bips.test.ts (existing Vitest pattern for schema tests in this repo)
    - lib/schemas/bip-wizard.ts (the exported step2Schema + fullBipSchema shapes to build valid base fixtures)
  </read_first>
  <behavior>
    - step2Schema accepts each of before/during/after/before_and_after/mixed for virtual_timing.
    - step2Schema rejects virtual_timing = 'concurrent'.
    - step2Schema rejects max_participants = 9 and accepts max_participants = 10.
    - step2Schema accepts a payload with virtual_sessions_count=4 and virtual_duration_notes set.
    - fullBipSchema accepts each of the 5 virtual_timing values, rejects 'concurrent', rejects max_participants 9, accepts 10.
    - fullBipSchema accepts a payload carrying accommodation_notes and partner_institutions_only=true.
  </behavior>
  <action>
    Create `tests/schemas/bip-wizard.test.ts`. Import `{ step2Schema, fullBipSchema }` from `@/lib/schemas/bip-wizard`. Build a `validStep2Base` and a `validFullBase` fixture object (all currently-required fields with valid values, e.g. dates ordered correctly, host_city, ects, study_levels, language fields, and for full: title/subject_areas/description/learning_outcomes + how_to_apply). Write the assertions in the `<behavior>` block using `.safeParse(...).success` toBe true/false. Run — the tests MUST fail (schema still has the old 3-value enum, .min(5), and no new-field validators).
  </action>
  <verify>
    <automated>npx vitest run tests/schemas/bip-wizard.test.ts; test $? -ne 0 && echo "RED-as-expected"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/schemas/bip-wizard.test.ts` exists and imports `step2Schema` and `fullBipSchema`.
    - `grep -c "before_and_after" tests/schemas/bip-wizard.test.ts` >= 1 and `grep -c "concurrent" tests/schemas/bip-wizard.test.ts` >= 1.
    - `npx vitest run tests/schemas/bip-wizard.test.ts` exits NON-zero (RED) before Task 2.
  </acceptance_criteria>
  <done>The failing test file exists and encodes the enum, floor, and new-field expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Fix the schema + extend the draft store</name>
  <read_first>
    - lib/schemas/bip-wizard.ts (VIRTUAL_TIMINGS const line 40; step2Schema max_participants line 64-68; fullBipSchema lines 152-198)
    - lib/store/bip-draft.ts (BipDraftData virtual_timing union line 39; field block lines 28-57)
    - lib/types/bip.ts (canonical `VirtualTiming` 5-value union to reuse)
  </read_first>
  <action>
    In `lib/schemas/bip-wizard.ts`:
    - Replace `const VIRTUAL_TIMINGS = ['before', 'after', 'concurrent'] as const` with `const VIRTUAL_TIMINGS = ['before', 'during', 'after', 'before_and_after', 'mixed'] as const`.
    - In step2Schema and fullBipSchema change `max_participants` `.min(5, ...)` → `.min(10, 'A BIP needs at least 10 participants.')` (keep the existing `.max(20)`).
    - Add to step2Schema AND fullBipSchema: `virtual_sessions_count: z.coerce.number().int().min(0).max(50).optional()` and `virtual_duration_notes: z.string().trim().max(500).optional().or(z.literal(''))`.
    - Add to step3Schema AND fullBipSchema: `partner_institutions_only: z.boolean().optional().default(false)`.
    - Add to step4Schema AND fullBipSchema: `accommodation_notes: z.string().trim().max(1000).optional().or(z.literal(''))`.

    In `lib/store/bip-draft.ts` BipDraftData: change `virtual_timing?: 'before' | 'after' | 'concurrent'` to `virtual_timing?: 'before' | 'during' | 'after' | 'before_and_after' | 'mixed'` (or import/reuse `VirtualTiming`). Add the four optional fields: `virtual_sessions_count?: number`, `virtual_duration_notes?: string`, `accommodation_notes?: string`, `partner_institutions_only?: boolean` (place virtual_* in the Step 2 block, partner_institutions_only in the Step 3 block, accommodation_notes in the Step 4 block to mirror D-01/D-02/D-03 placement).

    Run the unit tests — they MUST pass now.
  </action>
  <verify>
    <automated>npx vitest run tests/schemas/bip-wizard.test.ts && grep -Eq "before_and_after" lib/schemas/bip-wizard.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -vE '^\s*(//|\*)' lib/schemas/bip-wizard.ts | grep -c "concurrent"` returns 0 (no live 'concurrent' value remains).
    - `grep -c "before_and_after" lib/schemas/bip-wizard.ts` >= 1 and `grep -c "'mixed'" lib/schemas/bip-wizard.ts` >= 1.
    - `grep -Eo "virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only" lib/schemas/bip-wizard.ts | sort -u | wc -l` returns 4.
    - `grep -c "min(5" lib/schemas/bip-wizard.ts` returns 0; `grep -c "min(10" lib/schemas/bip-wizard.ts` >= 2.
    - `grep -vE '^\s*(//|\*)' lib/store/bip-draft.ts | grep -c "concurrent"` returns 0; `grep -Eo "virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only" lib/store/bip-draft.ts | sort -u | wc -l` returns 4.
    - `npx vitest run tests/schemas/bip-wizard.test.ts` exits 0 (GREEN).
  </acceptance_criteria>
  <done>Schema corrected + extended; draft store extended; unit tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Consolidate the create path onto fullBipSchema (Pitfall 0) + write new fields</name>
  <read_first>
    - lib/actions/bip-submit.ts (inline submitSchema lines 57-112; safeParse call line 138; updatePayload lines 195-231)
    - lib/schemas/bip-wizard.ts (the now-corrected fullBipSchema to import)
  </read_first>
  <action>
    In `lib/actions/bip-submit.ts`:
    - Delete the inline `const submitSchema = z.object({...}).refine(...)` block entirely.
    - Import `fullBipSchema` from `@/lib/schemas/bip-wizard` (add to the existing import line for step1Schema) and change `submitSchema.safeParse(draft)` to `fullBipSchema.safeParse(draft)`. Remove the now-unused `z` import only if nothing else uses it (keep it if `z` is still referenced).
    - Add the four new fields to `updatePayload` so a newly-submitted BIP persists them to `bips`: `virtual_sessions_count: parsed.data.virtual_sessions_count ?? null`, `virtual_duration_notes: parsed.data.virtual_duration_notes || null`, `accommodation_notes: parsed.data.accommodation_notes || null`, `partner_institutions_only: parsed.data.partner_institutions_only ?? false`.

    Run typecheck. The bips columns already exist in database.types.ts, so no dependency on Plan 09-01 here.
  </action>
  <verify>
    <automated>grep -q "fullBipSchema.safeParse" lib/actions/bip-submit.ts && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -vE '^\s*(//|\*)' lib/actions/bip-submit.ts | grep -c "const submitSchema"` returns 0 (inline duplicate removed).
    - `grep -c "fullBipSchema.safeParse" lib/actions/bip-submit.ts` returns 1.
    - `grep -Eo "virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only" lib/actions/bip-submit.ts | sort -u | wc -l` returns 4 (all four in updatePayload).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Create path validates via fullBipSchema (no hand-copied schema) and persists all four new fields on submit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client draft → submitBipAction | Untrusted wizard draft crosses into the public review queue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Tampering | bip-submit.ts server re-validation | mitigate | Server-side `fullBipSchema.safeParse` remains the trust boundary; consolidation preserves the identical cross-field refinements (date ordering, url-xor-contact) that the removed submitSchema enforced |
| T-09-02-02 | Tampering | virtual_timing value | mitigate | Enum now matches the DB CHECK exactly, so a crafted value that previously slipped past the wizard ('concurrent') is rejected before the DB write |
| T-09-02-03 | Info disclosure | new free-text fields | accept | virtual_duration_notes/accommodation_notes are plain-text, length-bounded, never rendered via dangerouslySetInnerHTML (project-wide no-rich-text rule) |
</threat_model>

<verification>
- `npx vitest run tests/schemas/bip-wizard.test.ts` exits 0
- `grep -vE '^\s*(//|\*)' lib/actions/bip-submit.ts | grep -c "const submitSchema"` == 0
- `grep -vE '^\s*(//|\*)' lib/schemas/bip-wizard.ts | grep -c "concurrent"` == 0
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
The two live bugs are fixed on every schema path, the four new fields are validatable and stored on the draft, the create path shares one schema with the edit path, and the create path writes all four fields to bips.
</success_criteria>

<output>
After completion, create `.planning/phases/09-coordinator-bip-builder-completion/09-02-SUMMARY.md`.
</output>
