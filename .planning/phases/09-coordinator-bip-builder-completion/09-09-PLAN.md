---
phase: 09-coordinator-bip-builder-completion
plan: 09
type: execute
wave: 5
depends_on: [03, 04, 07, 08]
files_modified:
  - tests/e2e/submission.spec.ts
  - tests/e2e/bip-edits.spec.ts
  - tests/e2e/bips-card.spec.ts
autonomous: true
requirements: [SUBM-09, SUBM-10, SUBM-11, SUBM-12, SUBM-13, SUBM-14, BROW-14]
must_haves:
  truths:
    - "A coordinator creating a BIP can set the new virtual/accommodation/partner fields and a valid virtual_timing option and submit without a DB CHECK error"
    - "Editing each new field on an already-approved BIP and having the admin approve persists the new value on the live bips row — proven per field by reading the live row back, not at render time (anti-Pitfall-1)"
    - "A /bips card shows the partner-only badge for a partner_institutions_only BIP and not for others"
  artifacts:
    - path: "tests/e2e/submission.spec.ts"
      provides: "Create-path assertions filling the new fields + a non-'before' virtual_timing option + max_participants 10"
      contains: "virtual"
    - path: "tests/e2e/bip-edits.spec.ts"
      provides: "Per-field edit→approve→persist assertions reading the live bips row via the service-role pattern (D-08)"
      contains: "partner_institutions_only"
    - path: "tests/e2e/bips-card.spec.ts"
      provides: "BROW-14 badge presence/absence assertion on /bips"
      contains: "Partner institutions only"
  key_links:
    - from: "tests/e2e/bip-edits.spec.ts read-back"
      to: "cloud bips row (service-role REST)"
      via: "assert each new field's post-approve value on e2e-edit-target-bip"
      pattern: "virtual_sessions_count|accommodation_notes"
---

<objective>
Prove the phase end-to-end: the create path accepts the new fields + a valid virtual_timing option (SUBM-09/10/11/12/13), each new field round-trips edit→approve→persist on an approved BIP verified by a live-row read-back (SUBM-14, D-08, anti-Pitfall-1), and the `/bips` partner-only badge renders correctly (BROW-14).

Purpose: SUBM-14's acceptance bar (D-08) is explicitly a per-field live-row read-back after admin approval — NOT a wizard-render or diff-render check. This plan encodes that binding proof plus the create-path and badge coverage. It is the phase gate.
Output: Extended create + edit specs and a new /bips card spec.
</objective>

<execution_context>
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-coordinator-bip-builder-completion/09-VALIDATION.md
@tests/e2e/bip-edits.spec.ts
@tests/e2e/submission.spec.ts
@tests/e2e/map-filter.spec.ts
@playwright.config.ts

<interfaces>
- E2E safety guard: `playwright.config.ts` refuses any Supabase target except local or cloud TEST ref `zbvcpiwbopmfbjfhzprw`. Confirm `.env.local` points at one before running. Serial (`workers: 1`).
- Read-back pattern (already in `bip-edits.spec.ts`): `assertAuditRow` uses `process.env.SUPABASE_SERVICE_ROLE_KEY` + a REST fetch to `.../rest/v1/bip_status_history?...`. Reuse the same fetch shape against `.../rest/v1/bips?id=eq.${E2E_BIP_ID}&select=virtual_sessions_count,virtual_duration_notes,accommodation_notes,partner_institutions_only` to read the LIVE row after approve.
- Edit-target fixture: `E2E_BIP_ID = 'e2e0bbbb-bbbb-bbbb-bbbb-000000000010'`, slug `e2e-edit-target-bip`, seeded (Plan 09-08) with known non-default values for all four new fields.
- Existing helpers to reuse: `driveEditWizardToStep5(page, bipId, title)`, the coordinator + admin auth fixtures, `assertAuditRow`.
- `map-filter.spec.ts` only asserts /bips navigation, not card content — so BROW-14 needs its OWN spec (`tests/e2e/bips-card.spec.ts`).
</interfaces>

<critical>
- SUBM-14 proof MUST read the live `bips` row after the admin approves the edit (per field), NOT the wizard preview or the diff view. A render-only assertion does not satisfy D-08.
- The edit-wizard drive must change each of the four fields to a NEW value distinct from the seeded value, so the read-back proves the merge actually applied.
- Keep specs in the existing serial describe blocks where they reuse `E2E_BIP_ID`/`assertAuditRow` to avoid fixture races.
</critical>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create-path assertions in submission.spec.ts</name>
  <read_first>
    - tests/e2e/submission.spec.ts (the 5-step wizard test lines 88-190; virtual_timing select interaction around line 128; step advance helpers lines 42-88)
  </read_first>
  <action>
    Extend the "coordinator submits a BIP through the 5-step wizard" test to: fill `virtual_sessions_count` and `virtual_duration_notes` in Step 2, choose a virtual_timing option that is NOT 'before' (e.g. 'before_and_after' or 'mixed') to exercise a previously-invalid path, set `max_participants` to 10 (the new floor), tick the partner-only checkbox in Step 3, and fill `accommodation_notes` in Step 4. Assert the wizard reaches submission without a validation/DB error (the submit succeeds and the dashboard reflects a pending BIP). This proves SUBM-12 (no CHECK violation on a non-'before' option) and SUBM-13 (10 accepted) through the create path.
  </action>
  <verify>
    <automated>grep -q "virtual_duration_notes\|virtual_sessions\|before_and_after\|mixed" tests/e2e/submission.spec.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The spec interacts with the new Step-2/3/4 controls: `grep -Eo "virtual_sessions|virtual_duration|accommodation|partner" tests/e2e/submission.spec.ts | sort -u | wc -l` >= 3.
    - The spec selects a non-'before' timing option: `grep -Eq "before_and_after|'mixed'|\"mixed\"|'during'|\"during\"" tests/e2e/submission.spec.ts`.
    - `npx playwright test tests/e2e/submission.spec.ts` passes (serial).
  </acceptance_criteria>
  <done>The create path accepts the new fields and a valid non-'before' timing option and submits cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Per-field edit→approve→persist read-back in bip-edits.spec.ts (D-08)</name>
  <read_first>
    - tests/e2e/bip-edits.spec.ts (assertAuditRow lines 55-90; driveEditWizardToStep5 lines 106-136; approve-edit test lines 278-311; E2E_BIP_ID line 41)
  </read_first>
  <action>
    Add a serial test (after the existing approve-edit flow, reusing `E2E_BIP_ID`) that: (1) as the coordinator, drives the edit wizard changing all four new fields to NEW values distinct from the seed (e.g. virtual_sessions_count 4→8, virtual_duration_notes to a new string, accommodation_notes to a new string, partner_institutions_only true→false — or false→true), submits the edit; (2) as the admin, approves the edit; (3) reads the LIVE `bips` row via the service-role REST pattern (`select=virtual_sessions_count,virtual_duration_notes,accommodation_notes,partner_institutions_only`) and asserts each field equals the NEW value. One assertion per field. This is the binding anti-Pitfall-1 proof (SUBM-14).
  </action>
  <verify>
    <automated>grep -q "partner_institutions_only" tests/e2e/bip-edits.spec.ts && grep -Eq "virtual_sessions_count|accommodation_notes" tests/e2e/bip-edits.spec.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The spec reads the live bips row for all four fields: `grep -Eo "virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only" tests/e2e/bip-edits.spec.ts | sort -u | wc -l` returns 4.
    - The read-back targets the `bips` table (not just bip_edits/bip_status_history): a REST fetch to `/rest/v1/bips?id=eq.` with a select of the new fields is present.
    - `npx playwright test tests/e2e/bip-edits.spec.ts` passes, including the new per-field assertions.
  </acceptance_criteria>
  <done>Each new field is proven to persist on the live bips row after an admin-approved edit.</done>
</task>

<task type="auto">
  <name>Task 3: BROW-14 badge spec on /bips</name>
  <read_first>
    - tests/e2e/map-filter.spec.ts (existing /bips navigation pattern to model the new spec on)
    - components/bip/BipCard.tsx (badge text "Partner institutions only")
  </read_first>
  <action>
    Create `tests/e2e/bips-card.spec.ts`. Navigate to `/bips`, and assert that the partner-only badge ("Partner institutions only") is visible on at least one card (a seeded partner_institutions_only=true BIP from Plan 09-08) and that the page also contains cards without the badge (badge is conditional, not global). Prefer filtering/locating a known partner-only seeded BIP by title if needed for determinism.
  </action>
  <verify>
    <automated>test -f tests/e2e/bips-card.spec.ts && grep -q "Partner institutions only" tests/e2e/bips-card.spec.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/bips-card.spec.ts` exists and navigates to `/bips`.
    - It asserts the badge text "Partner institutions only" is visible (>=1) AND asserts the badge is not on every card (conditional): the spec includes both a positive and a not-present/absence assertion.
    - `npx playwright test tests/e2e/bips-card.spec.ts` passes.
  </acceptance_criteria>
  <done>The partner-only badge is proven to render only for partner-only BIPs on /bips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → cloud TEST DB | Service-role read-backs and seeded fixtures on the guarded test project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09-01 | Info disclosure | SUPABASE_SERVICE_ROLE_KEY in specs | accept | Service-role key is env-injected in the e2e harness only (never in app/lib/components); reuses the existing assertAuditRow pattern already audited in Phase 8 |
| T-09-09-02 | Tampering | test target selection | mitigate | playwright.config guard refuses any Supabase target except local or the TEST ref — no production data reachable |
</threat_model>

<verification>
- `npx playwright test tests/e2e/submission.spec.ts tests/e2e/bip-edits.spec.ts tests/e2e/bips-card.spec.ts` passes (serial)
- `grep -Eo "virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only" tests/e2e/bip-edits.spec.ts | sort -u | wc -l` == 4
- Phase gate: `npm run test && npm run verify:seed` green, then the full `npx playwright test` green
</verification>

<success_criteria>
Create-path, per-field edit round-trip (live-row read-back), and the /bips badge are all proven by passing E2E specs.
</success_criteria>

<output>
After completion, create `.planning/phases/09-coordinator-bip-builder-completion/09-09-SUMMARY.md`.
</output>
