---
phase: 09-coordinator-bip-builder-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00022_bip_edits_builder_completion.sql
  - lib/supabase/database.types.ts
autonomous: true
requirements: [SUBM-09, SUBM-10, SUBM-11]
must_haves:
  truths:
    - "bip_edits table has the four builder-completion columns and a coordinator edit can persist them through the shadow-table flow"
    - "The regenerated database.types.ts reflects the four new bip_edits columns so all downstream select/insert code type-checks"
  artifacts:
    - path: "supabase/migrations/00022_bip_edits_builder_completion.sql"
      provides: "Additive migration adding virtual_sessions_count, virtual_duration_notes, accommodation_notes, partner_institutions_only to bip_edits"
      contains: "alter table public.bip_edits"
    - path: "lib/supabase/database.types.ts"
      provides: "Regenerated Supabase types with the four new bip_edits columns"
      contains: "virtual_sessions_count"
  key_links:
    - from: "supabase/migrations/00022_bip_edits_builder_completion.sql"
      to: "linked cloud Postgres (bip_edits table)"
      via: "supabase db push"
      pattern: "add column (virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only)"
    - from: "lib/supabase/database.types.ts"
      to: "bip_edits row/insert/update types"
      via: "supabase gen types (against the linked cloud project, NEVER --local)"
      pattern: "virtual_duration_notes|accommodation_notes"
---

<objective>
Add the four builder-completion columns to the `bip_edits` shadow table and regenerate the Supabase types against the linked cloud project. Every downstream plan that reads or writes `bip_edits` (P06, P07) depends on these columns existing in both the live database and `database.types.ts`.

Purpose: `bips` already carries all four columns (migration 00003); `bip_edits` carries none of them (migration 00017 + 00020). Without this migration the coordinator edit-and-re-review round trip (SUBM-14) silently drops every new field at the shadow-table layer.
Output: One additive migration, pushed to the linked cloud project, and a regenerated `database.types.ts`.
</objective>

<execution_context>
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-coordinator-bip-builder-completion/09-RESEARCH.md
@.planning/phases/09-coordinator-bip-builder-completion/09-CONTEXT.md
@supabase/migrations/00020_bip_subject_areas.sql
@supabase/migrations/00017_bip_edits.sql

<interfaces>
The four columns already exist on `bips` (migration 00003). Mirror the EXACT additive pattern migration 00020 used to add `subject_areas` to `bip_edits`: nullable, no default, no CHECK (Zod validates content at submit time — 00017's stated convention for content columns).

Confirmed current state (RESEARCH §Confirmed Field Status):
| Column                     | on bips | on bip_edits | notes |
| virtual_sessions_count     | yes     | MISSING      | integer |
| virtual_duration_notes     | yes     | MISSING      | text |
| accommodation_notes        | yes     | MISSING      | text |
| partner_institutions_only  | yes     | MISSING      | boolean (bips: not null default false; mirror as nullable no-default) |

No RLS change: bip_edits policies (00017) key on created_by/status, not column names.
No bips migration: all four already exist there.
</interfaces>

<critical>
- MEMORY / Pitfall 4: this project's local dev uses the shared cloud DB. `npm run db:types` runs `supabase gen types typescript --local` — DO NOT use it here. Build/type checks pass without the push because types come from config, not the live DB (a false-positive trap). You MUST push to the linked cloud project, then regenerate against that same linked project.
- Do NOT tighten the `max_participants` DB CHECK in this migration. Per RESEARCH Open Question 1, SUBM-13 is satisfied by the wizard/Zod floor (Plan 09-02); the DB CHECK is an unnamed inline column constraint (00003 line 27) whose retightening is out of scope for this phase.
</critical>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write additive bip_edits migration 00022</name>
  <read_first>
    - supabase/migrations/00020_bip_subject_areas.sql (the exact additive pattern to mirror — the bip_edits ALTER block, lines 28-29)
    - supabase/migrations/00017_bip_edits.sql (confirm bip_edits content-column conventions: nullable, no default)
  </read_first>
  <action>
    Create `supabase/migrations/00022_bip_edits_builder_completion.sql`. It contains exactly one additive statement mirroring 00020's bip_edits block:

    ```sql
    -- 00022_bip_edits_builder_completion.sql
    -- Mirror the four builder-completion columns from bips onto the bip_edits
    -- shadow table so coordinator edits round-trip through admin re-review.
    -- Nullable, no default, no CHECK (Zod validates at submit; matches 00017/00020
    -- content-column convention). No RLS change (policies key on created_by/status).
    alter table public.bip_edits
      add column virtual_sessions_count    integer,
      add column virtual_duration_notes    text,
      add column accommodation_notes       text,
      add column partner_institutions_only boolean;
    ```

    No backfill is needed (open edits are rare and these are net-new optional fields). No `bips` ALTER. No RLS/policy statements.
  </action>
  <verify>
    <automated>test -f supabase/migrations/00022_bip_edits_builder_completion.sql && grep -Eq "add column virtual_sessions_count" supabase/migrations/00022_bip_edits_builder_completion.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/00022_bip_edits_builder_completion.sql` exists.
    - `grep -c "add column" supabase/migrations/00022_bip_edits_builder_completion.sql` returns 4.
    - All four column names present: `grep -Eo "virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only" supabase/migrations/00022_bip_edits_builder_completion.sql | sort -u | wc -l` returns 4.
    - No `alter table public.bips` and no `enable row level security` / `create policy` lines: `grep -c "public.bips\|create policy\|row level security" supabase/migrations/00022_bip_edits_builder_completion.sql` returns 0.
  </acceptance_criteria>
  <done>Migration file exists, additive-only, four bip_edits columns, no bips/RLS statements.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push migration to linked cloud and regenerate types</name>
  <read_first>
    - package.json (confirm the `db:types` script uses `--local` — you must NOT invoke it as-is)
    - .planning/STATE.md (MEMORY note: local dev uses the shared cloud DB; push before regenerating types)
  </read_first>
  <action>
    This task is BLOCKING: no plan in Wave 2+ may run until it completes, because P06/P07 type-check against the regenerated bip_edits types.

    1. Push the migration to the linked cloud project:
       `supabase db push`
       (If prompted for auth or the project is not linked, surface an auth/link gate — do not fabricate credentials.)
    2. Regenerate types against the LINKED cloud project (NOT `--local`, NOT `npm run db:types`):
       `supabase gen types typescript --linked > lib/supabase/database.types.ts`
       (If `--linked` is unavailable in the installed CLI, use `--project-id <linked-ref>` for the same linked project.)
    3. Confirm the four new columns landed in the generated types under the `bip_edits` table's Row/Insert/Update.
  </action>
  <verify>
    <automated>grep -Eq "virtual_sessions_count" lib/supabase/database.types.ts && grep -Eq "partner_institutions_only" lib/supabase/database.types.ts && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` reports the migration applied (or "remote is up to date" on a re-run) with no error.
    - `lib/supabase/database.types.ts` contains all four column names: `grep -Eo "virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only" lib/supabase/database.types.ts | sort -u | wc -l` returns 4.
    - The regeneration was NOT done with `--local` (the four columns must reflect the pushed cloud schema).
    - `npx tsc --noEmit` exits 0 (regenerated types compile with existing code).
  </acceptance_criteria>
  <done>Migration is live on the linked cloud project and database.types.ts carries the four bip_edits columns; typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → cloud DDL | Schema change applied to the shared cloud database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | bip_edits new columns | accept | Columns are nullable content columns under the existing 00017 RLS (created_by/status scoped); Postgres RLS is row-scoped so new columns inherit the policy automatically — no new access-control surface |
| T-09-01-02 | Denial of Service | db push against shared cloud | mitigate | Additive-only ALTER (no rewrite, no lock-heavy backfill); mirrors the proven 00020 pattern |
</threat_model>

<verification>
- `grep -c "add column" supabase/migrations/00022_bip_edits_builder_completion.sql` == 4
- `grep -Eo "virtual_sessions_count|virtual_duration_notes|accommodation_notes|partner_institutions_only" lib/supabase/database.types.ts | sort -u | wc -l` == 4
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
The `bip_edits` table carries the four builder-completion columns on the linked cloud project, and `database.types.ts` reflects them so P06/P07 compile.
</success_criteria>

<output>
After completion, create `.planning/phases/09-coordinator-bip-builder-completion/09-01-SUMMARY.md`.
</output>
