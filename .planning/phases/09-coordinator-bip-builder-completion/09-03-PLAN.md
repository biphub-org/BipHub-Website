---
phase: 09-coordinator-bip-builder-completion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/queries/bips.ts
  - components/bip/BipCard.tsx
autonomous: true
requirements: [BROW-14]
must_haves:
  truths:
    - "A student browsing /bips sees a restrained amber badge on cards for BIPs that are open only to partner institutions"
    - "The badge appears only when partner_institutions_only is true; other cards look unchanged"
  artifacts:
    - path: "lib/queries/bips.ts"
      provides: "baseSelect includes partner_institutions_only so the card can read it"
      contains: "partner_institutions_only"
    - path: "components/bip/BipCard.tsx"
      provides: "Conditional partner-only badge using the existing status-pending token (static class strings)"
      contains: "Partner institutions only"
  key_links:
    - from: "lib/queries/bips.ts baseSelect"
      to: "components/bip/BipCard.tsx badge render"
      via: "bip.partner_institutions_only field flows from query to card"
      pattern: "partner_institutions_only"
---

<objective>
Surface a "Partner institutions only" badge on `/bips` cards for BIPs open only to partner-institution students (BROW-14). Treatment is noticeable but not alarming (D-06): reuse the existing amber `status-pending` design token, not a new colour and not a loud full-width warning.

Purpose: `lib/queries/bips.ts` `baseSelect` does not currently fetch `partner_institutions_only`, and `BipCard.tsx` has no badge — a student cannot tell a BIP may be closed to them before clicking through.
Output: The column added to the listing query and a conditional badge on the card.
</objective>

<execution_context>
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-coordinator-bip-builder-completion/09-CONTEXT.md
@lib/queries/bips.ts
@components/bip/BipCard.tsx
@lib/types/bip.ts

<interfaces>
`BipWithRelations` = `Bip & {...}` and `Bip` = `Database['public']['Tables']['bips']['Row']`, so `partner_institutions_only` is ALREADY on the type — no `lib/types/bip.ts` change needed. The runtime object only lacks it because `baseSelect` omits the column.

Existing amber token (app/globals.css): `--color-status-pending: #b45309`, `--color-status-pending-bg: #fffbeb`. It is already used for the "Pending" status pill elsewhere, so it reads as a known "heads-up" colour, not a new/error hue. CLAUDE.md never-do: NO dynamic Tailwind class names — use complete static class strings.

Reusable badge markup (RESEARCH Code Examples):
```tsx
{bip.partner_institutions_only && (
  <span className="inline-flex rounded-pill border border-status-pending bg-status-pending-bg px-2.5 py-1 text-[11px] font-semibold text-status-pending">
    Partner institutions only
  </span>
)}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add partner_institutions_only to the /bips listing query</name>
  <read_first>
    - lib/queries/bips.ts (baseSelect string lines 28-33)
  </read_first>
  <action>
    Add `partner_institutions_only,` to the `baseSelect` column list in `lib/queries/bips.ts` (e.g. append to the line that ends with `subject_areas,`). This is the only change in this file; the returned rows already map through `as BipWithRelations`, which already types the field.
  </action>
  <verify>
    <automated>grep -q "partner_institutions_only" lib/queries/bips.ts && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "partner_institutions_only" lib/queries/bips.ts` >= 1.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The /bips query fetches partner_institutions_only for every card.</done>
</task>

<task type="auto">
  <name>Task 2: Render the partner-only badge on BipCard</name>
  <read_first>
    - components/bip/BipCard.tsx (field tag chips block lines 112-124 — place the badge in the card body, near the field chips)
    - app/globals.css (confirm status-pending / status-pending-bg tokens exist — lines 144/148)
  </read_first>
  <action>
    In `components/bip/BipCard.tsx`, render the badge from the `<interfaces>` block when `bip.partner_institutions_only` is truthy. Place it in the card body (e.g. directly above or beside the field-tag chips at lines 112-124) so it sits with the BIP metadata, not over the gradient header. Use the exact static class string given — no template literals, no dynamic class construction. Keep the label text exactly `Partner institutions only`.
  </action>
  <verify>
    <automated>grep -q "Partner institutions only" components/bip/BipCard.tsx && grep -q "bg-status-pending-bg" components/bip/BipCard.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bip.partner_institutions_only" components/bip/BipCard.tsx` >= 1 (badge is conditional).
    - `grep -c "Partner institutions only" components/bip/BipCard.tsx` == 1.
    - `grep -c "text-status-pending" components/bip/BipCard.tsx` >= 1 and no template-literal class names introduced (no backtick-wrapped `className={\`...\`}` around Tailwind tokens): `grep -c "className={\`" components/bip/BipCard.tsx` returns 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Cards for partner-only BIPs show the amber badge; all others render unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → RSC render | Public read of an approved BIP's partner_institutions_only flag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Info disclosure | partner_institutions_only on public cards | accept | The flag is non-sensitive public metadata about the BIP's audience; surfacing it is the requirement (BROW-14) |
</threat_model>

<verification>
- `grep -c "partner_institutions_only" lib/queries/bips.ts` >= 1
- `grep -c "Partner institutions only" components/bip/BipCard.tsx` == 1
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
`/bips` cards display a restrained amber "Partner institutions only" badge only for BIPs where `partner_institutions_only` is true, using the existing status-pending token and static class strings.
</success_criteria>

<output>
After completion, create `.planning/phases/09-coordinator-bip-builder-completion/09-03-SUMMARY.md`.
</output>
