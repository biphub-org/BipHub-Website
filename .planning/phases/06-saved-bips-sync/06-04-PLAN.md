---
phase: 06-saved-bips-sync
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - tests/e2e/saved-bips.spec.ts
  - playwright.config.ts
  - supabase/seed.e2e.sql
autonomous: true
requirements: [STUD-04, STUD-05, STUD-07, STUD-08, FOUN-09, FOUN-10]
must_haves:
  truths:
    - "A save persists across a full page reload (server-side, not localStorage) — STUD-04/STUD-05"
    - "Unsaving from the saved list removes the BIP from the list — STUD-04"
    - "A saved BIP is visible at /student-dashboard/saved with current metadata — STUD-07"
    - "After account deletion there are zero saved_bips rows for the deleted user (direct SQL) — FOUN-09/STUD-08"
    - "/privacy contains the saved_bips enumeration text — FOUN-10"
    - "npm run build succeeds with /bips ISR and /privacy static intact"
  artifacts:
    - path: "tests/e2e/saved-bips.spec.ts"
      provides: "Playwright E2E coverage for STUD-04/05/07, STUD-08, FOUN-09, FOUN-10"
      contains: "saved_bips"
    - path: "playwright.config.ts"
      provides: "project wiring so saved-bips.spec.ts runs with a student session"
      contains: "saved-bips"
  key_links:
    - from: "tests/e2e/saved-bips.spec.ts"
      to: "saved_bips (REST)"
      via: "service-role read after deletion asserts zero rows"
      pattern: "saved_bips"
    - from: "playwright.config.ts"
      to: "tests/e2e/saved-bips.spec.ts"
      via: "testMatch on the student project"
      pattern: "saved-bips"
---

<objective>
Close the Nyquist gap: create the Playwright E2E spec proving the Phase 6 success criteria the unit test cannot, wire it into the Playwright project list, and run the full suite + production build as the phase gate. This makes STUD-04, STUD-05, STUD-07, STUD-08, FOUN-09, and FOUN-10 test-provable. STUD-06's pure core is already proven by the Plan 01 unit test (D-02a) — do NOT write an E2E against a non-existent v1.0 bookmark UI.

Purpose: SC1 (persist after reload), SC2 (cross-device = re-read after fresh session), SC4 (one list), SC5 (no orphan rows by direct SQL), SC6 (privacy enumeration) must be proven automatically before `/gsd-verify-work`.
Output: `tests/e2e/saved-bips.spec.ts`, updated `playwright.config.ts`, optional idempotent seed touch, green full suite + build.
</objective>

<execution_context>
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-saved-bips-sync/06-CONTEXT.md
@.planning/phases/06-saved-bips-sync/06-RESEARCH.md
@.planning/phases/06-saved-bips-sync/06-PATTERNS.md
@.planning/phases/06-saved-bips-sync/06-01-SUMMARY.md
@.planning/phases/06-saved-bips-sync/06-02-SUMMARY.md
@.planning/phases/06-saved-bips-sync/06-03-SUMMARY.md

<interfaces>
<!-- Existing E2E infra (tests/e2e/student-auth.spec.ts) — REUSE verbatim: -->
const STUDENT_EMAIL = 'e2e-student@biphub.test'   // password: 'Student!Test1'
async function signInStudent(page: Page): Promise<void>   // injects sb-{ref}-auth-token cookie, navigates to /student-dashboard
async function getStudentSession(request): Promise<{ accessToken, studentId, anonKey }>  // OTP exchange, ANON key

<!-- playwright.config.ts current student project (lines 96-102): -->
{ name: 'student-authed', testMatch: /student-auth\.spec\.ts$/, use: { ...devices['Desktop Chrome'] } }
<!-- webServer runs `npm run build && npm run start` against localhost:3000; retries 0; workers 1; fullyParallel false -->
<!-- SAFETY GUARD: suite refuses to run against the prod cloud ref unless E2E_ALLOW_CLOUD=1 -->

<!-- Service-role read for the no-orphan assertion (FOUN-09): -->
process.env.SUPABASE_SERVICE_ROLE_KEY  // tests only — post-deletion audit read (RLS bypass is correct for this assertion)

<!-- Routes under test: /bips (heart on card), /student-dashboard/saved (unsave), /privacy -->
<!-- SaveToggleIsland aria-label: "Save {title}" / "Unsave {title}"; aria-pressed true/false -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire the Playwright project for saved-bips.spec.ts</name>
  <read_first>
    - playwright.config.ts (full file — projects list, student-authed project at lines 96-102, safety guard, webServer)
    - tests/e2e/student-auth.spec.ts lines 36-110 (helpers — STUDENT_EMAIL, signInStudent, getStudentSession)
    - .planning/phases/06-saved-bips-sync/06-RESEARCH.md Open Question 3 (project decision) + lines 738-745 (Wave 0)
    - supabase/seed.e2e.sql (existing student fixture shape — only if a pre-seeded save row is needed)
  </read_first>
  <files>playwright.config.ts, supabase/seed.e2e.sql</files>
  <action>
DECISION (resolving RESEARCH Open Question 3): EXTEND the existing `student-authed` project's `testMatch` to also match `saved-bips.spec.ts`, rather than adding a new project. Rationale: the new spec uses the same student fixture (`e2e-student@biphub.test`) and the same `signInStudent` session strategy; a second project would duplicate config and risk fixture-cookie races (workers=1, fullyParallel=false).

Change the student-authed `testMatch` from `/student-auth\.spec\.ts$/` to `/(student-auth|saved-bips)\.spec\.ts$/`. Do NOT change retries, workers, fullyParallel, the safety guard, or the webServer block.

Seed: the spec saves a BIP as part of its own flow (no pre-seeded save row required), so `supabase/seed.e2e.sql` should need NO change. Prefer reading an existing approved BIP id at test runtime via the REST API over seeding a `saved_bips` row. ONLY if a guaranteed-approved BIP id is not reliably present do you touch the seed — and then keep it idempotent (delete-first or ON CONFLICT) and match the real migration 00003 schema (host_city, physical_start_date, ects_credits, how_to_apply_type/value, host_university_id — NOT invented columns, per the Plan 04-07 contract in STATE.md). Do NOT seed a saved_bips row that another spec might collide with.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "saved-bips" playwright.config.ts && grep -q "student-auth|saved-bips" playwright.config.ts && grep -q "retries: 0" playwright.config.ts && grep -q "workers: 1" playwright.config.ts && echo CONFIG_OK</automated>
  </verify>
  <acceptance_criteria>
    - `playwright.config.ts` `student-authed` project `testMatch` matches both `student-auth.spec.ts` and `saved-bips.spec.ts` (contains `student-auth|saved-bips`)
    - `retries: 0`, `workers: 1`, and the prod-ref safety guard are unchanged
    - If `supabase/seed.e2e.sql` was edited, it remains idempotent and matches the real 00003 schema; otherwise it is unchanged
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>saved-bips.spec.ts is wired into the student-authed project; no flake-retry or parallelism regressions; seed untouched (or idempotent if edited).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: tests/e2e/saved-bips.spec.ts — STUD-04/05/07, STUD-08, FOUN-09, FOUN-10</name>
  <read_first>
    - tests/e2e/student-auth.spec.ts (full file — header docblock style, signInStudent, getStudentSession, semantic selectors getByRole/getByLabel/getByText, API-level RLS assertion pattern at lines 273-299)
    - .planning/phases/06-saved-bips-sync/06-RESEARCH.md lines 718-745 (test map) + 660-666 (A3: e2e-student fixture)
    - .planning/phases/06-saved-bips-sync/06-UI-SPEC.md (aria-labels "Save {title}"/"Unsave {title}", detail button labels, empty-state copy, privacy copy "Saved BIPs")
    - .planning/phases/06-saved-bips-sync/06-CONTEXT.md D-02a (NO E2E for STUD-06 — unit test only)
    - tests/e2e/EDGE-CASES-DEFERRED.md (destructive-fixture contract: e2e-coordinator-fresh is consumed by auth.spec.ts; pick a separate throwaway for the deletion test)
  </read_first>
  <files>tests/e2e/saved-bips.spec.ts</files>
  <action>
Create `tests/e2e/saved-bips.spec.ts`. Lead with a docblock listing the SC/REQ coverage and the D-02a note (STUD-06 is unit-tested in `tests/schemas/saved-bips.test.ts`, NOT here — no v1.0 bookmark UI to drive). Copy the `signInStudent` and `getStudentSession` helpers from `student-auth.spec.ts` (duplicate verbatim if they are not exported, matching the established pattern). Use semantic selectors only (`getByRole`, `getByLabel`, `getByText`) — no className targeting.

Tests to implement:

1. **STUD-04 / STUD-05 — save persists across reload.** `signInStudent(page)` → `page.goto('/bips')` → click the heart on the first card (`page.getByRole('button', { name: /^Save / }).first()`) → assert it flips to the saved state (`aria-pressed="true"` or the `/^Unsave /` accessible name) → `page.reload()` → assert the same card's heart is STILL saved after reload. This proves server-side persistence (not localStorage).

2. **STUD-04 — unsave removes from the saved list.** `page.goto('/student-dashboard/saved')` → confirm at least one saved card is present → click its filled heart (`page.getByRole('button', { name: /^Unsave / }).first()`) → assert the card is removed (the card title / the unsave button is no longer visible). Design assertions to be order-independent and leave the fixture re-runnable (workers=1, retries=0): re-save at the end or assert on a card the test itself saved.

3. **STUD-05 — cross-device parity.** After saving in test 1, clear cookies and call `signInStudent(page)` again (simulates device B) → `page.goto('/student-dashboard/saved')` → assert the previously-saved BIP appears. Server-side state survives a fresh session.

4. **STUD-07 — one list with current metadata.** On `/student-dashboard/saved`, assert the saved BIP's title text is visible (metadata read live from `bips`).

5. **STUD-08 / FOUN-09 — no orphan rows after account deletion (direct REST/SQL assertion).** Use a THROWAWAY student created via the admin API (do NOT delete `e2e-student@biphub.test` — other tests depend on it; honor the EDGE-CASES-DEFERRED destructive-fixture contract). Flow: create/sign-in a dedicated deletable student → save a BIP → assert exactly one `saved_bips` row for that user_id via a SERVICE-ROLE REST read (`SUPABASE_SERVICE_ROLE_KEY`, RLS-bypass is correct for an audit assertion) → drive the `DeleteAccountDialog` on `/student-dashboard` (open modal, type the account email, click the destructive confirm) → assert redirect to `/?deleted=1` → read `saved_bips` again via the service-role key filtered by that user_id → assert ZERO rows. If creating a throwaway student is not feasible in the env, perform the cascade assertion at the API layer against a freshly admin-created user and its saved_bips rows, and document any limitation in the SUMMARY.

6. **FOUN-10 — privacy enumeration.** `page.goto('/privacy')` → `expect(page.getByText('Saved BIPs')).toBeVisible()` and assert the `saved_bips` / "cascading deletion" copy is present via `getByText`.

Respect the safety guard: run against the local/test Supabase target (or set E2E_ALLOW_CLOUD=1 only for a dedicated test project). Do NOT add a STUD-06 E2E.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f tests/e2e/saved-bips.spec.ts && grep -q "saved_bips" tests/e2e/saved-bips.spec.ts && grep -qE "STUD-04|STUD-05|STUD-07|FOUN-09|FOUN-10" tests/e2e/saved-bips.spec.ts && grep -q "/privacy" tests/e2e/saved-bips.spec.ts && grep -q "SUPABASE_SERVICE_ROLE_KEY" tests/e2e/saved-bips.spec.ts && ! grep -q "STUD-06" tests/e2e/saved-bips.spec.ts && echo SPEC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/saved-bips.spec.ts` exists and references `saved_bips`
    - Covers STUD-04, STUD-05, STUD-07, FOUN-09, FOUN-10 (req IDs appear in the docblock)
    - Contains a `/privacy` assertion and a `SUPABASE_SERVICE_ROLE_KEY` post-deletion no-orphan read
    - Does NOT contain a `STUD-06` E2E (unit-tested per D-02a)
    - Uses a throwaway/admin-created student for the deletion test (does not destroy `e2e-student@biphub.test`)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Spec proves the five E2E-provable criteria with semantic selectors; deletion test uses a throwaway student and asserts zero orphan rows via service-role read; no STUD-06 E2E.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Phase gate — full unit + build, confirm ISR/static invariants</name>
  <read_first>
    - package.json scripts (test = vitest run; build = next build; test:e2e = playwright test)
    - .planning/phases/06-saved-bips-sync/06-RESEARCH.md Pitfall 6 (/privacy force-static) + Pitfall 7 (/bips ISR)
  </read_first>
  <files>tests/e2e/saved-bips.spec.ts</files>
  <action>
Run the phase gate and capture results in the SUMMARY:

1. `npm test` (Vitest unit suite — must be green, includes the Plan 01 `saved-bips.test.ts`).
2. `npm run build` — must succeed. Inspect the route table in the build output and confirm:
   - `/privacy` renders as static (`○`) — Pitfall 6. If it flipped to dynamic (`ƒ`), the privacy edit (Plan 03) introduced dynamic behavior — fix before finalizing.
   - `/bips` remains ISR/static-eligible. If reading claims (Plan 02) flipped `/bips` to fully dynamic (`ƒ`) and that is unacceptable, gate the claims read so anon requests skip it; otherwise document the accepted dynamic transition in the SUMMARY (RESEARCH A2).
3. Run the E2E suite against the configured test target: `npm run test:e2e` (or the student project: `npx playwright test --project=student-authed`). All Phase 6 specs must pass. If the env cannot reach a test Supabase, record exactly which assertions ran vs. were environment-blocked in the SUMMARY (do NOT silently mark green).

This task modifies no production code; it is the gate. If any check fails, fix in the owning plan's file (or here for test-only adjustments) and re-run. Record the final route table (static/dynamic markers for /bips and /privacy) and the suite result in `06-04-SUMMARY.md`.
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (Vitest green, incl. saved-bips unit test)
    - `npm run build` exits 0
    - Build route table shows `/privacy` static (○) and `/bips` not regressed below its prior ISR behavior (or the transition is documented in the SUMMARY)
    - `npm run test:e2e` (or `--project=student-authed`) result is recorded in the SUMMARY with any env-blocked assertions called out explicitly
  </acceptance_criteria>
  <done>Unit suite green, production build succeeds, /privacy static + /bips ISR confirmed, E2E result recorded honestly in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → Supabase (service-role) | The no-orphan audit read uses the service-role key to bypass RLS — correct for a test assertion, must never leak into app code |
| E2E suite → Supabase target | The safety guard prevents the destructive suite from running against the prod cloud project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-18 | Tampering | E2E running against prod cloud, polluting live data | mitigate | Existing playwright.config safety guard (refuses prod ref unless E2E_ALLOW_CLOUD=1) is preserved unchanged |
| T-06-19 | Repudiation | false-positive green (cascade never verified) | mitigate | FOUN-09 test reads saved_bips via service-role AFTER deletion and asserts zero rows — the cascade is observed, not assumed |
| T-06-20 | Information Disclosure | service-role key committed in the spec | accept | Key is read from `process.env.SUPABASE_SERVICE_ROLE_KEY` (never literal); gitleaks workflow (Plan 04-04) guards commits |
</threat_model>

<verification>
- `npx tsc --noEmit` passes after Tasks 1–2
- `npm test` green; `npm run build` succeeds; route table confirms /privacy static + /bips ISR
- `npm run test:e2e` (student project) passes against the test target, or env-blocked assertions are documented
- All grep gates pass
</verification>

<success_criteria>
- saved-bips.spec.ts proves STUD-04, STUD-05, STUD-07, STUD-08, FOUN-09, FOUN-10 with semantic selectors
- FOUN-09 cascade verified by a direct service-role read (zero orphan rows)
- STUD-06 left to the Plan 01 unit test (D-02a) — no E2E against a non-existent UI
- Full unit suite + production build green; ISR/static invariants intact
</success_criteria>

<output>
After completion, create `.planning/phases/06-saved-bips-sync/06-04-SUMMARY.md`
</output>
