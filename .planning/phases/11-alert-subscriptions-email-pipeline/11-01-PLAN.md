---
phase: 11-alert-subscriptions-email-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00042_bip_subscriptions_and_deliveries.sql
  - supabase/migrations/00043_enable_pg_net_and_approved_at_trigger.sql
  - supabase/config.toml
  - lib/constants/bip-alerts.ts
  - lib/supabase/database.types.ts
autonomous: true
requirements: [ALRT-07, ALRT-08, ALRT-09, FOUN-11, FOUN-12]
must_haves:
  truths:
    - "bip_subscriptions and bip_alert_deliveries exist with FK ON DELETE CASCADE, unique(bip_id,user_id) on deliveries, and approved_at backfilled on approved BIPs"
    - "pg_net extension is enabled (DB + config.toml) and approved_at trigger fires only on pending→approved"
    - "RLS on bip_subscriptions is owner USING + WITH CHECK (no user_id reassign) and bip_alert_deliveries is RLS-enabled service-role-only, and database.types.ts reflects all new columns"
  artifacts:
    - path: "supabase/migrations/00042_bip_subscriptions_and_deliveries.sql"
      provides: "Creates bip_subscriptions + bip_alert_deliveries + bips.approved_at with RLS and indexes"
      contains: "create table public.bip_subscriptions"
    - path: "supabase/migrations/00043_enable_pg_net_and_approved_at_trigger.sql"
      provides: "Enables pg_net and creates set_approved_at() trigger"
      contains: "create extension if not exists pg_net"
    - path: "supabase/config.toml"
      provides: "Local parity for pg_net"
      contains: "pg_net = true"
    - path: "lib/constants/bip-alerts.ts"
      provides: "Frequency enum, cap, consent text"
      contains: "ALERT_CONSENT_TEXT"
  key_links:
    - from: "supabase/migrations/00042_bip_subscriptions_and_deliveries.sql"
      to: "linked cloud Postgres"
      via: "supabase db push --linked"
      pattern: "create table public.bip_subscriptions"
    - from: "lib/supabase/database.types.ts"
      to: "generated types"
      via: "supabase gen types typescript --linked"
      pattern: "bip_subscriptions"
---

<objective>
Create the two new PII tables, the approved_at high-water-mark marker, the pg_net extension, and the approved→approved_at trigger, plus local config parity and the alert constants. This is the blocking DDL for all later alert work — no Server Action, Edge Function, or UI lands without it.

Purpose: ALRT-07 idempotency must key on bips.approved_at (never updated_at, which edit-merges bump — Pitfall 3). FOUN-11 requires owner USING + WITH CHECK on bip_subscriptions and no public policies on bip_alert_deliveries. FOUN-12 requires ON DELETE CASCADE so delete_my_account cascades.
Output: Two additive migrations pushed to linked cloud, config.toml updated, constants file, regenerated database.types.ts.
</objective>

<execution_plan>
Plan 11-01 is file-scaffolding complete but NOT yet pushed. Next steps are verification-only:

1. `supabase db push --linked` — pushes 00042 + 00043 to cloud TEST project (zbvcpiwbopmfbjfhzprw). Must succeed before any type regen.
2. `SELECT extname FROM pg_extension WHERE extname='pg_net'` — true on both local and cloud.
3. `SELECT approved_at FROM bips WHERE status='approved' LIMIT 5` — non-null (backfill worked).
4. `npm run db:types` — regen against --linked (NEVER --local per Pitfall 2), then `npx tsc --noEmit` should be green for bip_subscriptions types (WizardStep2 10 errors remain pre-existing, tracked separately).
5. `npm run lint` — 0 errors.
</execution_plan>

<tasks>
- [x] Create 00042_bip_subscriptions_and_deliveries.sql (already written, uncommitted)
- [x] Create 00043_enable_pg_net_and_approved_at_trigger.sql (already written, uncommitted)
- [x] Patch supabase/config.toml [db.extensions] pg_net = true (already written, uncommitted)
- [x] Create lib/constants/bip-alerts.ts (already written, uncommitted)
- [ ] Push to cloud and regenerate types (BLOCKING — requires `supabase link` + cloud creds, do NOT run without user confirmation per git safety)
- [ ] Verify RLS: `SELECT relname, relrowsecurity FROM pg_class WHERE relname IN ('bip_subscriptions','bip_alert_deliveries')` → both true
</tasks>

<validation>
- `supabase db push --dry-run --linked` — shows 2 pending migrations (dry-run, no write)
- After real push: `npx supabase gen types typescript --linked | grep bip_subscriptions` — shows new table
- `npm run test` — 110/110 still green (no new tests yet)
</validation>
