---
phase: 11-alert-subscriptions-email-pipeline
plan: 03
type: execute
wave: 2
depends_on: ["11-02"]
files_modified:
  - supabase/functions/send-bip-alerts/index.ts
  - lib/email/templates/AlertDigest.tsx
  - lib/constants/unsubscribe.ts
autonomous: true
requirements: [ALRT-03, ALRT-05, ALRT-07]
must_haves:
  truths:
    - "Edge Function anti-joins on approved_at to find newly-approved matching BIPs, reserves bip_alert_deliveries with onConflictDoNothing, sends Resend batch with per-item List-Unsubscribe headers, and second consecutive run sends 0 emails"
---

<objective>
Build the Edge Function that does the actual digest: anti-join matcher, HMAC token generation, reserve-then-send idempotency, Resend batch. Depends on the pg_cron gate (11-02) being green — otherwise there is nothing to schedule.

Details: Deno service-role supabase-js, approved_at anti-join + NOT EXISTS deliveries, per-subscription field/country match, HMAC-SHA256 token via node:crypto timingSafeEqual, List-Unsubscribe + List-Unsubscribe-Post headers per recipient, resend.batch.send up to 100/call with 500ms delay.
</objective>

<tasks>
- [ ] Scaffold supabase/functions/send-bip-alerts via `npx supabase functions new send-bip-alerts`
- [ ] Implement index.ts (Deno, see parent plan Key Decisions: reserve-then-send, approved_at high-water)
- [ ] Create lib/email/templates/AlertDigest.tsx (React Email)
- [ ] Create lib/constants/unsubscribe.ts (HMAC helpers)
- [ ] Unit tests for HMAC + matcher
</tasks>

<validation>
- `supabase functions serve --env-file .env.local` + two manual curls → second run 0 sends (idempotency)
- `resend.batch.send` spy shows List-Unsubscribe headers
</validation>
