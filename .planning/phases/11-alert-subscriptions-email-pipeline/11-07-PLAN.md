---
phase: 11-alert-subscriptions-email-pipeline
plan: 07
type: execute
wave: 4
depends_on: ["11-06"]
files_modified:
  - app/(public)/privacy/page.tsx
  - tests/e2e/alert-subscriptions.spec.ts
autonomous: true
requirements: [FOUN-12, FOUN-13, ALRT-04]
must_haves:
  truths:
    - "/privacy enumerates subscription + alert-delivery surfaces, delete_my_account cascades both tables (throwaway user delete → 0 rows), and full E2E covers create→digest→unsubscribe→cascade"
---

<objective>
GDPR + privacy + full E2E. Extend /privacy (storage-surface enumeration pattern from Plan 04-02), verify ON DELETE CASCADE (no RPC edit needed), and write the throwaway-student E2E spec that proves the whole pipeline (never mutates e2e-student@biphub.test).
</objective>

<tasks>
- [ ] Update /privacy page (add bip_subscriptions + bip_alert_deliveries paragraphs)
- [ ] Verify DELETE CASCADE: create throwaway via admin API, create subs, delete user, assert 0 rows
- [ ] Write tests/e2e/alert-subscriptions.spec.ts (create → edit → delete → no-login unsubscribe → digest header check)
</tasks>

<validation>
- `DELETE FROM auth.users WHERE id=throwaway` → both tables 0 rows
- Playwright student-authed + throwaway: full pipeline green
- /privacy snapshot test
</validation>
