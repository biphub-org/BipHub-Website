---
phase: 11-alert-subscriptions-email-pipeline
plan: 04
type: execute
wave: 2
depends_on: ["11-03"]
files_modified:
  - lib/actions/bip-subscriptions.ts
  - lib/schemas/bip-subscriptions.ts
  - lib/queries/subscriptions.ts
autonomous: true
requirements: [ALRT-01, ALRT-02, ALRT-08, ALRT-09, FOUN-11]
must_haves:
  truths:
    - "Student can create subscription (field and/or country, weekly default) storing explicit consent_text, and 6th active subscription is rejected; update frequency and delete work with RLS WITH CHECK preventing user_id reassign; cross-user write is 403"
---

<objective>
Server Actions for subscription lifecycle. All use getClaims() and RLS WITH CHECK. Cap enforced in action (not CHECK) for friendly error. Depends on Edge Function contract (approved_at) already proven.
</objective>

<tasks>
- [ ] Create Zod schema for field/country/frequency + consent
- [ ] Implement create/update/delete/list actions with getClaims() + cap 5 + consent_text write
- [ ] RLS WITH CHECK prevents reassign: try UPDATE user_id to other user → 403/PGRST
- [ ] Unit + RLS tests
</tasks>

<validation>
- `npm run test` + manual RLS check (cross-user update fails)
- 6th create → error "Maximum 5 subscriptions"
</validation>
