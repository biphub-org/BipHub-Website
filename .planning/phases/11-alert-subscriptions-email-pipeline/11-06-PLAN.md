---
phase: 11-alert-subscriptions-email-pipeline
plan: 06
type: execute
wave: 3
depends_on: ["11-05"]
files_modified:
  - app/api/unsubscribe/route.ts
  - app/(public)/unsubscribe/page.tsx
autonomous: true
requirements: [ALRT-05, ALRT-06, FOUN-11]
must_haves:
  truths:
    - "Anonymous GET /api/unsubscribe?token= with valid HMAC deletes the subscription; invalid token is 403; token is capability not session-bound"
---

<objective>
No-login unsubscribe via HMAC. Public Route Handler (one sanctioned exception to Server Actions for all mutations) verifies token with timingSafeEqual then deletes via service-role. Document the exception with eslint-disable comment.
</objective>

<tasks>
- [ ] Implement /api/unsubscribe Route Handler (public GET, HMAC verify, service-role delete)
- [ ] Add confirmation page
- [ ] Playwright: create as student A, fetch token from DB, anonymous GET deletes; invalid token 403
</tasks>

<validation>
- Anonymous GET with valid token → 200 + row deleted
- Invalid token → 403
</validation>
