---
phase: 11-alert-subscriptions-email-pipeline
plan: 02
type: execute
wave: 1
depends_on: ["11-01"]
files_modified:
  - supabase/migrations/00044_schedule_bip_digest.sql
  - scripts/verify-cron.ts
autonomous: false
requirements: [FOUN-11]
must_haves:
  truths:
    - "pg_cron job exists scheduling daily and weekly digests via pg_net.http_post to the Edge Function, and cron.job_run_details shows succeeded on the cloud TEST project"
  artifacts:
    - path: "supabase/migrations/00044_schedule_bip_digest.sql"
      provides: "Schedules daily + weekly pg_cron jobs pointing at the Edge Function"
      contains: "cron.schedule"
---

<objective>
Wire pg_cron to pg_net so the digest Edge Function is actually scheduled. This is the infrastructure-first gate (Pitfall 4/6) — no Server Action or UI counts as done without a real cron.job_run_details.succeeded row on the cloud TEST project.

Purpose: Local pg_cron cannot reach a public URL, so the schedule is a cloud-only proof. The plan scaffolds the schedule migration and the verify-cron.ts script that queries cron.job_run_details directly (not a deferred manual check).
</objective>

<tasks>
- [ ] Create 00044_schedule_bip_digest.sql (daily 08:00 UTC + weekly Monday 08:00 UTC, Vault secret header, extensions schema)
- [ ] Create scripts/verify-cron.ts (queries cron.job and cron.job_run_details, prints jobname + status + start_time)
- [ ] After push, confirm on cloud: SELECT * FROM cron.job WHERE jobname LIKE 'bip_digest%' and SELECT * FROM cron.job_run_details ORDER BY start_time DESC LIMIT 1 → status='succeeded'
</tasks>

<validation>
- `supabase db push --linked` then `npx tsx scripts/verify-cron.ts` → prints 2 jobs + last run succeeded
- Manual `supabase functions serve` + `curl` not required for this gate — gate is cloud cron firing, not local
</validation>
