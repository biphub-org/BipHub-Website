---
phase: 08-edit-approved-request-changes
plan: 07
type: execute
wave: 4
depends_on: [08-03, 08-04, 08-05]
files_modified:
  - components/dashboard/EditStatusCallout.tsx
  - app/(dashboard)/dashboard/bips/[id]/edit/page.tsx
autonomous: true
requirements: [EDIT-01, EDIT-09]
user_setup: []

must_haves:
  truths:
    - "On an approved BIP with no open edit, the coordinator sees a 'Submit Edit for Review' CTA and the slug field is absent from the DOM"
    - "On an approved BIP with a pending edit, the CTA is the disabled 'Edit in review'"
    - "On an open edit in changes_requested, the admin note shows and the CTA is 'Resubmit Edit'"
    - "Submitting an edit calls submitEditAction and shows the success toast; resubmit (edit or new-submission) passes the wizard draft + partners so content edits are preserved"
  artifacts:
    - path: "components/dashboard/EditStatusCallout.tsx"
      provides: "approved/pending/changes_requested status banner"
      contains: "Changes requested"
    - path: "app/(dashboard)/dashboard/bips/[id]/edit/page.tsx"
      provides: "approved-BIP edit flow with State A/B/C CTAs + slug omission"
      contains: "Submit Edit for Review"
  key_links:
    - from: "edit page State A CTA"
      to: "submitEditAction"
      via: "client action call → success toast → State B"
      pattern: "submitEditAction"
    - from: "edit page (approved)"
      to: "slug field omission"
      via: "slug input not rendered when status==='approved' (EDIT-09/D-10)"
      pattern: "approved"
---

<objective>
Wave 4 (parallel with 08-08): the coordinator entry point. Extend the existing dashboard edit page so an approved BIP no longer 404s but instead renders the three edit states (A/B/C) with the right CTA, prefilled from the live content or the open edit, and with the slug field removed entirely (dual-guard half of EDIT-09; the server guard is in 08-05).

Purpose: Implement D-09 (reuse existing form) + UI-SPEC Surface 1 + the EDIT-01 coordinator UX.
Output: EditStatusCallout component + extended edit page.
</objective>

<execution_context>
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-edit-approved-request-changes/08-UI-SPEC.md
@.planning/phases/08-edit-approved-request-changes/08-PATTERNS.md

<interfaces>
From 08-04: getCoordinatorBipById returns CoordinatorBipForEdit { id, data: BipDraftData (live), status, openEdit?: { id, status:'pending'|'changes_requested', admin_note, data } | null }; getLatestChangesRequest(bipId) for the new-submission changes_requested note.
From 08-05: submitEditAction(bipId, draft, partners), resubmitEditAction(editId, draft, partners), resubmitPendingBipAction(bipId, draft, partners) — ALL three carry the wizard draft + partners.
Analog callout: components/dashboard/DashboardBipCard.tsx lines 79-100 (gold left-border rejected-reason callout). Icons: CheckCircle2, Clock (lucide-react).
UI-SPEC Surface 1 copy (verbatim): State A info "This BIP is live. Submit an edit to propose changes — the public page stays unchanged until an admin approves."; CTA "Submit Edit for Review" (gold pill `bg-eu-gold text-ink hover:bg-eu-gold/90 rounded-pill px-5 py-2 font-semibold w-full`); State B callout "Your edit is under review. You can update fields below, but resubmission isn't available until the admin responds." + disabled CTA "Edit in review"; State C heading "Changes requested" + footer "Update your submission below, then resubmit for review." + CTA "Resubmit Edit". Toasts: submit "Edit submitted for review. The public page stays live while it's being reviewed."; resubmit "Edit resubmitted. An admin will review it shortly." Error Alert: "Your edit could not be submitted. Try again or contact support."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create components/dashboard/EditStatusCallout.tsx</name>
  <read_first>
    - components/dashboard/DashboardBipCard.tsx (lines 79-100 — gold left-border callout analog)
    - .planning/phases/08-edit-approved-request-changes/08-UI-SPEC.md (Surface 1 States A/B/C markup)
    - .planning/phases/08-edit-approved-request-changes/08-PATTERNS.md (EditStatusCallout section)
  </read_first>
  <files>components/dashboard/EditStatusCallout.tsx</files>
  <action>
'use client'. Props { status: 'approved' | 'pending' | 'changes_requested'; adminNote?: string | null }. Render exactly one callout per UI-SPEC Surface 1:
  - 'approved' (State A): `rounded-md border border-eu-blue-100 bg-eu-blue-50 px-4 py-3 mb-6 flex items-start gap-2`; CheckCircle2 size16 text-status-approved mt-0.5 flex-shrink-0; text-sm text-ink = the State A info copy.
  - 'pending' (State B): `rounded-md border border-status-pending bg-status-pending-bg px-4 py-3 mb-6 flex items-start gap-2`; Clock size16 text-status-pending; text = State B copy.
  - 'changes_requested' (State C): `rounded-r border-l-4 border-eu-gold bg-eu-gold/5 px-4 py-3 mb-6`; heading "Changes requested" `text-sm font-semibold text-ink mb-1`; body {adminNote} `text-sm text-ink-2 mb-2` (text content only, no HTML injection); footer copy `text-xs text-muted`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "EditStatusCallout" | awk '{ if ($1==0) print "ok"; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "'use client'" components/dashboard/EditStatusCallout.tsx
    - grep -q "Changes requested" AND grep -q "This BIP is live" AND grep -q "under review" components/dashboard/EditStatusCallout.tsx
    - grep -q "border-l-4 border-eu-gold" components/dashboard/EditStatusCallout.tsx (State C)
    - grep -q "adminNote" components/dashboard/EditStatusCallout.tsx (rendered as text)
  </acceptance_criteria>
  <done>Three-state coordinator callout matching Surface 1; admin note rendered as text in State C.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the coordinator edit page for approved/changes_requested BIPs</name>
  <read_first>
    - app/(dashboard)/dashboard/bips/[id]/edit/page.tsx (current getCoordinatorBipById usage + 404 on null; wizard mount + Submit CTA)
    - components/dashboard/EditStatusCallout.tsx (Task 1)
    - lib/actions/bip-edits.ts (submitEditAction / resubmitEditAction / resubmitPendingBipAction from 08-05 — all take draft + partners)
    - lib/queries/bipEdits.ts (getLatestChangesRequest)
    - .planning/phases/08-edit-approved-request-changes/08-UI-SPEC.md (Surface 1 interaction contract + CTA classes)
  </read_first>
  <files>app/(dashboard)/dashboard/bips/[id]/edit/page.tsx</files>
  <action>
The page now renders (not 404s) for approved + changes_requested BIPs (08-04 widened the whitelist). Determine the render state from CoordinatorBipForEdit:
  - status==='approved' && !openEdit → State A: mount EditStatusCallout status="approved"; prefill wizard from live `data`; bottom CTA "Submit Edit for Review" → on click call submitEditAction(bipId, draft, partners); success → Sonner submit toast + reflect State B (router.refresh()).
  - status==='approved' && openEdit?.status==='pending' → State B: EditStatusCallout status="pending"; prefill from openEdit.data; disabled CTA "Edit in review" (`variant="outline" disabled rounded-pill px-5 py-2 w-full opacity-60 cursor-not-allowed`); resubmit action NOT mounted.
  - openEdit?.status==='changes_requested' → State C: EditStatusCallout status="changes_requested" adminNote={openEdit.admin_note}; prefill from openEdit.data; CTA "Resubmit Edit" → resubmitEditAction(openEdit.id, draft, partners); success → resubmit toast.
  - status==='changes_requested' && !openEdit → NEW-SUBMISSION D-06a path: fetch getLatestChangesRequest(bipId) for the note; EditStatusCallout status="changes_requested" adminNote={note}; prefill from live `data`; CTA "Resubmit Edit" → resubmitPendingBipAction(bipId, draft, partners). CRITICAL: pass the wizard `draft` + `partners` form values (mirror EXACTLY the State C edit call resubmitEditAction(openEdit.id, draft, partners)) so the coordinator's revised content is persisted — do NOT call a status-only/no-arg variant.
SLUG OMISSION (EDIT-09/D-10): when status==='approved' (States A/B/C edit flow), the wizard MUST NOT render the slug field/input — pass a prop to the wizard (e.g. hideSlug / omitSlug) or conditionally skip the slug field so no input[name=slug] appears in the DOM. Server error from any action → inline `Alert variant="destructive"` with the error copy above the CTA.
Keep the existing draft/pending/rejected behaviour unchanged (this only adds the approved + changes_requested branches).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "bips/\[id\]/edit/page" | awk '{ if ($1==0) print "ok"; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Submit Edit for Review" AND grep -q "Resubmit Edit" AND grep -q "Edit in review" app/(dashboard)/dashboard/bips/[id]/edit/page.tsx
    - grep -q "submitEditAction" AND "resubmitEditAction" AND "resubmitPendingBipAction" in the page (or its mounted client child)
    - resubmitPendingBipAction is called with draft + partners args (grep -Eq "resubmitPendingBipAction\\([^)]*draft[^)]*partners|resubmitPendingBipAction\\(bipId, *draft, *partners") — NOT a no-arg/status-only call
    - grep -q "EditStatusCallout" app/(dashboard)/dashboard/bips/[id]/edit/page.tsx
    - the page passes a slug-omission flag when status==='approved' (grep for the hideSlug/omitSlug prop wired to status==='approved')
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Approved + changes_requested BIPs render the correct State A/B/C CTA, prefilled appropriately, with the slug field removed for approved-BIP edits; new-submission resubmit forwards draft + partners (no content loss).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| coordinator browser → edit submit | Draft content + slug-omission enforced client-side (server re-guards) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-20 | Tampering | slug field reappearing in approved-BIP edit DOM | mitigate | Wizard omits slug input when status==='approved'; server merge also omits slug (08-05) — dual guard EDIT-09 |
| T-08-21 | Information Disclosure | admin note HTML injection in State C | mitigate | EditStatusCallout renders adminNote as text content only |
| T-08-22 | Elevation | coordinator forcing resubmit on a pending edit (State B) | mitigate | Resubmit action not mounted in State B; server resubmit pinned to changes_requested→pending (08-05/RLS) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for both files
- `npx playwright test tests/e2e/bip-edits.spec.ts -g "submit edit"` and `-g "slug immutable"` should now pass for the coordinator State A + slug-absence assertions (pending Wave 1 push applied)
</verification>

<success_criteria>
Coordinator can submit/resubmit an edit from the existing edit form across States A/B/C with no slug field on approved BIPs (EDIT-01, EDIT-09 client half); new-submission resubmit preserves revised content.
</success_criteria>

<output>
After completion, create `.planning/phases/08-edit-approved-request-changes/08-07-SUMMARY.md`
</output>
