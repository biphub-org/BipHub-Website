---
phase: 08-edit-approved-request-changes
plan: 03
type: execute
wave: 2
depends_on: [08-02]
files_modified:
  - lib/utils/status.ts
  - lib/utils/status-transitions.ts
  - app/globals.css
  - lib/email/templates/EditApprovalEmail.tsx
  - lib/email/templates/EditRejectionEmail.tsx
  - lib/email/templates/EditChangesRequestedEmail.tsx
  - lib/email/send.ts
autonomous: true
requirements: [EDIT-06, EDIT-07]
user_setup: []

must_haves:
  truths:
    - "BipStatus union includes 'changes_requested' and renders an amber badge via a literal class lookup (no template literals)"
    - "ALLOWED_TRANSITIONS includes the four new-submission changes_requested transitions"
    - "sendEmail handles edit-approved, edit-rejected, edit-changes-requested with compile-time exhaustiveness"
    - "edit-rejected and edit-changes-requested email props carry adminNote rendered verbatim with pre-wrap"
  artifacts:
    - path: "lib/utils/status.ts"
      provides: "changes_requested in BipStatus + STATUS_BADGE_CLASSES + STATUS_LABELS"
      contains: "changes_requested"
    - path: "lib/email/send.ts"
      provides: "3 new EmailPayload variants + resolveSubject + switch cases"
      contains: "edit-approved"
    - path: "lib/email/templates/EditRejectionEmail.tsx"
      provides: "rejection email embedding admin note"
      contains: "adminNote"
  key_links:
    - from: "STATUS_BADGE_CLASSES.changes_requested"
      to: "app/globals.css --color-status-changes-requested"
      via: "literal Tailwind class string"
      pattern: "bg-status-changes-requested-bg"
    - from: "sendEmail switch"
      to: "Edit*Email templates"
      via: "exhaustive discriminated union"
      pattern: "edit-changes-requested"
---

<objective>
Wave 2 (parallel with 08-04): build the cross-cutting status + email foundation. Extend the status vocabulary with changes_requested (D-06a) and add the three note-bearing outcome emails (D-11/EDIT-07). The exhaustive EmailPayload union forces compile-time coverage so no outcome can ship without a template.

Purpose: Provide the shared primitives every later UI/action wave consumes — the amber changes_requested badge token and the three Edit* email variants.
Output: extended status utils + globals token + 3 email templates + extended send.ts.
</objective>

<execution_context>
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-edit-approved-request-changes/08-UI-SPEC.md
@.planning/phases/08-edit-approved-request-changes/08-PATTERNS.md

<interfaces>
Current EmailPayload (lib/email/send.ts lines 33-36):
  | { template: 'approval'; props: ApprovalEmailProps }
  | { template: 'rejection'; props: RejectionEmailProps }
  | { template: 'admin-notification'; props: AdminNotificationEmailProps }
Both resolveSubject() (lines 42-56) and sendEmail() switch (lines 66-83) end in a `never` exhaustiveness check that errors until all cases are handled.

Current BipStatus (lib/utils/status.ts line 15): 'draft'|'pending'|'approved'|'rejected'
Current ALLOWED_TRANSITIONS (lib/utils/status-transitions.ts lines 24-32): 7 entries.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend status vocabulary + globals token + transitions</name>
  <read_first>
    - lib/utils/status.ts (BipStatus, STATUS_BADGE_CLASSES, STATUS_LABELS)
    - lib/utils/status-transitions.ts (ALLOWED_TRANSITIONS, validateTransition)
    - app/globals.css (@theme inline block; existing status-pending amber token)
    - .planning/phases/08-edit-approved-request-changes/08-UI-SPEC.md (Color section — exact CSS + safelist comment)
  </read_first>
  <behavior>
    - validateTransition('pending','changes_requested','admin') === true
    - validateTransition('changes_requested','pending','coordinator') === true
    - validateTransition('changes_requested','approved','admin') === true
    - validateTransition('changes_requested','rejected','admin') === true
    - STATUS_BADGE_CLASSES.changes_requested is a complete literal string (no backticks/${})
  </behavior>
  <action>
1) lib/utils/status.ts: add 'changes_requested' to the BipStatus union; add to STATUS_BADGE_CLASSES the LITERAL string `changes_requested: 'bg-status-changes-requested-bg text-status-changes-requested border-status-changes-requested',` (no template literals — Tailwind v4 / CLAUDE.md never-do); add `changes_requested: 'Changes Requested',` to STATUS_LABELS.
2) app/globals.css: inside the @theme inline block, add the Phase 8 comment block + tokens from UI-SPEC Color section verbatim, including the safelist comment listing bg-status-changes-requested-bg / text-status-changes-requested / border-status-changes-requested:
     --color-status-changes-requested: #b45309;
     --color-status-changes-requested-bg: #fffbeb;
3) lib/utils/status-transitions.ts: append 4 entries to ALLOWED_TRANSITIONS:
     { from: 'pending', to: 'changes_requested', actor: 'admin' },
     { from: 'changes_requested', to: 'pending', actor: 'coordinator' },
     { from: 'changes_requested', to: 'approved', actor: 'admin' },
     { from: 'changes_requested', to: 'rejected', actor: 'admin' },
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "status.ts|status-transitions.ts" | head -5; grep -q "changes_requested: 'bg-status-changes-requested-bg text-status-changes-requested border-status-changes-requested'" lib/utils/status.ts && grep -q -- "--color-status-changes-requested:" app/globals.css && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "changes_requested" lib/utils/status.ts in BipStatus, STATUS_BADGE_CLASSES, STATUS_LABELS (3 occurrences min)
    - STATUS_BADGE_CLASSES.changes_requested contains no `${` and no backtick
    - grep -c "changes_requested" lib/utils/status-transitions.ts >= 4
    - grep -q "bg-status-changes-requested-bg" app/globals.css (safelist comment present)
  </acceptance_criteria>
  <done>changes_requested status fully wired: type, literal badge class, label, css tokens, 4 transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Create 3 Edit outcome email templates</name>
  <read_first>
    - lib/email/templates/ApprovalEmail.tsx (structure, tokens import, EC disclaimer footer)
    - lib/email/templates/RejectionEmail.tsx (required-note callout lines 123-155, gold left border, whiteSpace pre-wrap)
    - lib/email/tokens.ts (EMAIL_TOKENS / T)
    - .planning/phases/08-edit-approved-request-changes/08-PATTERNS.md (EditApprovalEmail / EditRejectionEmail / EditChangesRequestedEmail sections — exact copy strings)
  </read_first>
  <files>lib/email/templates/EditApprovalEmail.tsx, lib/email/templates/EditRejectionEmail.tsx, lib/email/templates/EditChangesRequestedEmail.tsx</files>
  <action>
EditApprovalEmail.tsx — props EditApprovalEmailProps { bipTitle, bipSlug, coordinatorName, siteOrigin? }. Preview "Your BIP edit is live on BipHub"; H1 "Your BIP edit is live on BipHub"; body "Your edit to &ldquo;{bipTitle}&rdquo; has been approved and the updated version is now live."; CTA "View your BIP" → `${siteOrigin}/bip/${bipSlug}` (default siteOrigin https://biphub.eu). No note block. MANDATORY EC disclaimer footer "Independent project — not affiliated with the European Commission".

EditRejectionEmail.tsx — props EditRejectionEmailProps { bipTitle, bipSlug, coordinatorName, adminNote, siteOrigin? }. Copy the RejectionEmail note callout (gold left border, backgroundColor bgSoft, whiteSpace 'pre-wrap') labelled "Reviewer feedback" rendering {adminNote} verbatim. Body: edit was rejected, live BIP stays unchanged. CTA → `${siteOrigin}/dashboard/bips/${bipSlug}/edit`. EC disclaimer footer.

EditChangesRequestedEmail.tsx — props EditChangesRequestedEmailProps { bipTitle, bipSlug, coordinatorName, adminNote, siteOrigin? }. Structurally identical to EditRejectionEmail with amber framing: H1 "Changes requested on your BIP edit"; body "Your edit to &ldquo;{bipTitle}&rdquo; requires changes before it can go live."; note callout label "Changes requested" rendering {adminNote} pre-wrap; CTA label "Review and resubmit →" → `${siteOrigin}/dashboard/bips/${bipSlug}/edit`. EC disclaimer footer.
  </action>
  <verify>
    <automated>node -e "['EditApprovalEmail','EditRejectionEmail','EditChangesRequestedEmail'].forEach(f=>{const c=require('fs').readFileSync('lib/email/templates/'+f+'.tsx','utf8');if(!/not affiliated with the European Commission/.test(c)){console.error('missing disclaimer '+f);process.exit(1)}});const r=require('fs').readFileSync('lib/email/templates/EditRejectionEmail.tsx','utf8');const cr=require('fs').readFileSync('lib/email/templates/EditChangesRequestedEmail.tsx','utf8');if(!/adminNote/.test(r)||!/pre-wrap/.test(r)||!/adminNote/.test(cr)){process.exit(1)}console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files export their Props interface and a default/named component
    - EditRejectionEmail + EditChangesRequestedEmail both reference `adminNote` and `whiteSpace`/`pre-wrap`
    - All 3 contain the EC disclaimer string (CLAUDE.md never-do)
    - EditApprovalEmail has NO adminNote prop
  </acceptance_criteria>
  <done>Three React-email templates: approval (no note), rejection (note), changes-requested (note, amber), all with EC disclaimer.</done>
</task>

<task type="auto">
  <name>Task 3: Extend EmailPayload union + resolveSubject + sendEmail switch</name>
  <read_first>
    - lib/email/send.ts (EmailPayload union lines 33-36; resolveSubject switch lines 42-56; sendEmail switch lines 66-83; never-exhaustiveness checks)
    - lib/email/templates/EditApprovalEmail.tsx + EditRejectionEmail.tsx + EditChangesRequestedEmail.tsx (Task 2 props/exports)
  </read_first>
  <files>lib/email/send.ts</files>
  <action>
Import the 3 new templates + their Props types. Extend EmailPayload with:
  | { template: 'edit-approved'; props: EditApprovalEmailProps }
  | { template: 'edit-rejected'; props: EditRejectionEmailProps }
  | { template: 'edit-changes-requested'; props: EditChangesRequestedEmailProps }
Add resolveSubject() cases:
  edit-approved → "Your BIP edit is live"
  edit-rejected → "Your BIP edit was not approved"
  edit-changes-requested → "Changes requested on your BIP edit"
Add sendEmail() switch cases rendering each template with render(). The terminal `never` exhaustiveness check must now compile with all 6 templates handled (the build error before adding cases is the proof of coverage).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "send.ts" | awk '{ if ($1==0) print "ok"; else { print "FAIL tsc errors in send.ts"; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "edit-approved\|edit-rejected\|edit-changes-requested" lib/email/send.ts >= 6 (union + subject + switch)
    - `npx tsc --noEmit` reports zero errors referencing send.ts (exhaustive switch satisfied)
  </acceptance_criteria>
  <done>send.ts compiles with all 6 templates; 3 new subjects + 3 new render cases wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| admin note → coordinator email body | Admin-authored text rendered into an outbound email |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Information Disclosure | XSS/HTML injection via admin note in email | mitigate | @react-email renders {adminNote} as text content (no dangerouslySetInnerHTML); whiteSpace pre-wrap only |
| T-08-09 | Tampering | dynamic Tailwind class for changes_requested badge | mitigate | Literal STATUS_BADGE_CLASSES entry + globals safelist comment (Pitfall 13) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for the touched files
- grep assertions for status + email tokens pass
</verification>

<success_criteria>
changes_requested badge + 4 transitions exist; three note-bearing Edit emails compile under the exhaustive union (EDIT-07 template layer ready; EDIT-06 amber status ready).
</success_criteria>

<output>
After completion, create `.planning/phases/08-edit-approved-request-changes/08-03-SUMMARY.md`
</output>
