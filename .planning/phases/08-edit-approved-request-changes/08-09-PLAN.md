---
phase: 08-edit-approved-request-changes
plan: 09
type: execute
wave: 5
depends_on: [08-07, 08-08]
files_modified: []
autonomous: false
requirements: [EDIT-04, EDIT-07]
user_setup: []

must_haves:
  truths:
    - "The full Playwright suite is green (bip-edits + no regressions in admin-review/saved-bips)"
    - "A real Resend edit-outcome email is delivered with a working BIP link"
    - "Approved-edit content appears on the public page within seconds of the verdict (ISR revalidatePath)"
  artifacts: []
  key_links:
    - from: "approveEditAction revalidatePath"
      to: "public /bip/[slug]"
      via: "ISR bust visible within seconds"
      pattern: "revalidatePath"
---

<objective>
Wave 5: the manual-only verification gate. Per 08-VALIDATION.md the two behaviours the automated E2E cannot assert — real Resend delivery (EDIT-07) and the "within seconds" ISR refresh perception (EDIT-04) — are confirmed by a human after the full suite is green.

Purpose: Close EDIT-04/EDIT-07 with the perceptual + live-integration checks the headless E2E intentionally does not cover.
Output: human sign-off recorded in the SUMMARY.
</objective>

<execution_context>
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-edit-approved-request-changes/08-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full suite + build gate</name>
  <read_first>
    - .planning/phases/08-edit-approved-request-changes/08-VALIDATION.md (Manual-Only Verifications table)
    - playwright.config.ts (projects)
  </read_first>
  <files></files>
  <action>
Run `npm run build && npx playwright test`. The build must succeed and the full suite must be green — bip-edits.spec.ts (all 8) plus the existing admin-review.spec.ts and saved-bips.spec.ts with no regressions (the union admin queue + AdminActionsPanel changes touch shared admin surfaces). Capture the pass summary into the SUMMARY. If anything is red, stop and report the failing test rather than proceeding to the human checkpoint.
  </action>
  <verify>
    <automated>npm run build && npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `npx playwright test` reports 0 failures; bip-edits.spec.ts shows 8 passed
  </acceptance_criteria>
  <done>Build green; full Playwright suite green including all 8 bip-edits tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full Phase 8 edit/re-review loop is wired and the automated suite is green. This checkpoint confirms the two manual-only items from 08-VALIDATION: real Resend email delivery (EDIT-07) and the perceptual "within seconds" ISR refresh after approve-edit (EDIT-04).</what-built>
  <how-to-verify>
    1. Ensure RESEND_API_KEY is set for the running environment.
    2. As a coordinator, open an approved BIP's edit page, change the title, and click "Submit Edit for Review". Confirm the success toast and that /bip/[slug] still shows the ORIGINAL title (EDIT-02).
    3. As an admin, open the queue, click the "Edit"-badged item, confirm the diff highlights the changed title, and click "Approve Edit".
    4. Reload /bip/[slug] and confirm the new title appears within a few seconds (EDIT-04 ISR).
    5. Confirm the coordinator inbox received the "Your BIP edit is live" email with a working BIP link (EDIT-07).
    6. Submit and reject a second edit with a note; confirm the live BIP is unchanged and the rejection email arrives with the note embedded.
    7. Request changes on a third edit; confirm the coordinator dashboard edit page shows the note (State C) and "Resubmit Edit" works.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| live Resend → coordinator inbox | Real outbound email with admin-authored note |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-26 | Information Disclosure | admin note rendered unsafely in delivered email | mitigate | Verified in 08-03 (text-only @react-email render); human confirms note renders as plain text |
</threat_model>

<verification>
- `npm run build && npx playwright test` green
- Human confirms Resend delivery + ISR refresh + reject/request-changes loop
</verification>

<success_criteria>
Full suite green; EDIT-04 (within-seconds refresh) and EDIT-07 (real email delivery) confirmed by human sign-off.
</success_criteria>

<output>
After completion, create `.planning/phases/08-edit-approved-request-changes/08-09-SUMMARY.md`
</output>
