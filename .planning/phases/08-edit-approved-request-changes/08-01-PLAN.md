---
phase: 08-edit-approved-request-changes
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/e2e/bip-edits.spec.ts
  - playwright.config.ts
  - supabase/seed.e2e.sql
autonomous: true
requirements: [EDIT-01, EDIT-02, EDIT-03, EDIT-04, EDIT-05, EDIT-06, EDIT-07, EDIT-08, EDIT-09]
user_setup: []

must_haves:
  truths:
    - "A failing E2E spec tests/e2e/bip-edits.spec.ts exists covering EDIT-01..EDIT-09 by grep-name"
    - "playwright.config.ts admin-authed project testMatch includes bip-edits"
    - "supabase/seed.e2e.sql contains a status='approved' BIP owned by e2e-coordinator@biphub.test plus a pending bip_edits row"
  artifacts:
    - path: "tests/e2e/bip-edits.spec.ts"
      provides: "Wave 0 failing spec for the whole phase"
      min_lines: 120
    - path: "playwright.config.ts"
      provides: "testMatch wiring for bip-edits on admin-authed project"
      contains: "bip-edits"
    - path: "supabase/seed.e2e.sql"
      provides: "approved BIP + pending bip_edits seed rows"
      contains: "bip_edits"
  key_links:
    - from: "tests/e2e/bip-edits.spec.ts"
      to: "playwright admin-authed project"
      via: "testMatch /(admin-review|bip-edits)\\.spec\\.ts$/"
      pattern: "bip-edits"
---

<objective>
Wave 0: create the failing end-to-end harness for Phase 8 BEFORE any feature code. Per 08-VALIDATION.md, this spec + seed + Playwright wiring is the Nyquist feedback instrument every later wave samples against.

Purpose: Lock the observable success criteria (EDIT-01..EDIT-09) as executable assertions up front. The spec is expected to be RED until the feature waves land — that is correct.
Output: tests/e2e/bip-edits.spec.ts, extended playwright.config.ts, extended supabase/seed.e2e.sql.
</objective>

<execution_context>
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-edit-approved-request-changes/08-VALIDATION.md
@.planning/phases/08-edit-approved-request-changes/08-PATTERNS.md
@.planning/phases/08-edit-approved-request-changes/08-UI-SPEC.md

<interfaces>
Playwright projects (playwright.config.ts, verified):
- coordinator-authed: testMatch /(submission|resubmit)\.spec\.ts$/ , storageState tests/e2e/fixtures/storageState.coordinator.json
- admin-authed: testMatch /admin-review\.spec\.ts$/ , storageState tests/e2e/fixtures/storageState.admin.json

DESIGN DECISION (avoid double-execution): bip-edits.spec.ts is assigned ONLY to the
admin-authed project. Coordinator-side assertions (submit/resubmit/public-page-unchanged)
spawn a fresh browser context with storageState 'tests/e2e/fixtures/storageState.coordinator.json'
INSIDE the spec — exactly the multi-context pattern used in admin-review.spec.ts test 3.
Do NOT add bip-edits to the coordinator-authed testMatch (that would run every test twice).

Real editable BIP columns (slug EXCLUDED — D-10). bip_edits seed row mirrors these:
title, isced_f_code, description, learning_outcomes, virtual_component_description,
virtual_timing, host_city, physical_start_date, physical_end_date, application_deadline,
ects_credits, max_participants, study_levels(text[]), language_of_instruction,
language_level_min, green_travel, inclusion_support, eligibility_notes,
how_to_apply_type, how_to_apply_value, contact_name, contact_email,
partner_institutions(jsonb). NOTE: the bip_edits table does NOT exist until Plan 08-02
is pushed — the seed's bip_edits INSERT is applied at test run time (after Wave 1 push), so
it is safe to author now.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the failing bip-edits E2E spec</name>
  <read_first>
    - tests/e2e/admin-review.spec.ts (analog — copy header, console-capture + multi-context pattern from test 3)
    - .planning/phases/08-edit-approved-request-changes/08-VALIDATION.md (Per-Task Verification Map — the -g grep names below MUST match)
    - .planning/phases/08-edit-approved-request-changes/08-UI-SPEC.md (Copywriting Contract — assert exact button/toast/badge strings)
  </read_first>
  <files>tests/e2e/bip-edits.spec.ts</files>
  <action>
Create tests/e2e/bip-edits.spec.ts (admin-authed project). Mirror admin-review.spec.ts structure: console-message capture array + `page.on('console', m => msgs.push(m.text()))`, outcome-based assertions (Server Action logs go to server stdout, not browser — assert UI/DB outcomes, not console for D-15).

Author exactly these test() titles so the VALIDATION -g grep names resolve (each MUST contain the quoted phrase):
  1. test('coordinator submits edit for approved BIP — submit edit', ...) — spawn coordinator context (storageState.coordinator.json); goto the seeded approved BIP edit page; assert button name /submit edit for review/i visible (State A); fill a field; click CTA; assert Sonner toast "Edit submitted for review. The public page stays live while it's being reviewed."; assert State B disabled "Edit in review".
  2. test('public page unchanged while edit pending — public page unchanged', ...) — public context; goto /bip/[seeded-slug]; assert original title still rendered (NOT the proposed edit title).
  3. test('admin sees diff view — diff view', ...) — admin context; goto /admin; click the "Edit"-badged card review link; expect URL /\/admin\/bip-edits\/.+\/review/; assert getByText('Field Comparison') visible.
  4. test('admin approves edit — approve edit', ...) — admin; open edit review; click /^approve edit$/i; confirm in modal; wait /\/admin/; then public reload shows merged title.
  5. test('admin rejects edit — reject edit', ...) — admin; open a pending edit; Reject Edit + required note; assert live BIP unchanged on public page.
  6. test('admin requests changes on new submission — request changes new submission', ...) — admin; open a pending NEW submission review (/admin/bips/[id]/review); click Request Changes; fill note; assert status badge "Changes Requested".
  7. test('admin requests changes on approved-BIP edit — request changes edit', ...) — admin; open edit review; Request Changes; note; assert outcome.
  8. test('slug immutable through edit flow — slug immutable', ...) — coordinator context; on approved-BIP edit page assert NO input with name/id 'slug' is present in the DOM; (and after approve in test 4, public slug URL unchanged).

Use `test.describe.configure({ mode: 'serial' })` where ordering matters (submit before diff/approve). Use a service-role DB read helper (mirror admin-review.spec.ts pattern) to assert EDIT-08: a bip_status_history row with the expected action_kind ('submit_edit','approve_edit','reject_edit','request_changes') exists after each action.

It is acceptable and expected that this spec is RED now (selectors/routes do not exist yet). Wire selectors to the UI-SPEC copy so later waves turn it green.
  </action>
  <verify>
    <automated>npx playwright test --list tests/e2e/bip-edits.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --list tests/e2e/bip-edits.spec.ts` lists 8 tests with zero syntax errors
    - grep -c "submit edit\|public page unchanged\|diff view\|approve edit\|reject edit\|request changes new submission\|request changes edit\|slug immutable" tests/e2e/bip-edits.spec.ts returns >= 8
    - grep -q "storageState.coordinator.json" tests/e2e/bip-edits.spec.ts (multi-context for coordinator assertions)
  </acceptance_criteria>
  <done>Spec file exists, lists 8 tests, all 8 VALIDATION grep-names present, coordinator context spawned in-spec.</done>
</task>

<task type="auto">
  <name>Task 2: Wire playwright.config.ts + seed the approved BIP and pending edit</name>
  <read_first>
    - playwright.config.ts (admin-authed project at the 'admin-review' testMatch line)
    - supabase/seed.e2e.sql (existing 20-BIP + e2e-coordinator pattern; match migration 00003 columns — STATE.md notes the real field names: host_city, physical_start_date, ects_credits, how_to_apply_type/value, host_university_id)
    - supabase/migrations/00003_bips_full_schema.sql (authoritative editable columns)
  </read_first>
  <files>playwright.config.ts, supabase/seed.e2e.sql</files>
  <action>
1) playwright.config.ts: change ONLY the admin-authed project testMatch from `/admin-review\.spec\.ts$/` to `/(admin-review|bip-edits)\.spec\.ts$/`. Leave coordinator-authed testMatch UNCHANGED (bip-edits runs admin-only + spawns coordinator context).

2) supabase/seed.e2e.sql: append two deterministic rows owned by e2e-coordinator@biphub.test (look up its auth user id the same way existing seed rows do):
   a) One bips row: status='approved', a fixed slug e.g. 'e2e-edit-target-bip' and a fixed uuid (use a literal uuid constant) so the spec can navigate by id/slug. Populate all required NOT-NULL columns mirroring an existing seed BIP. This BIP must NOT be one consumed by admin-review.spec.ts (do not reuse "Machine Learning Foundations").
   b) One bip_edits row: status='pending', bip_id = the approved BIP's uuid, created_by = e2e-coordinator's auth uid, with proposed content that differs from the live BIP in at least the title (e.g. title='[EDIT] E2E Edit Target BIP — revised') so the diff view shows a changed field and the "approve edit" test can assert the merged title. partner_institutions = '[]'::jsonb. Wrap in a guarded delete-then-insert (mirror existing seed idempotency: delete from bip_edits where bip_id = <uuid> first).
   Add a header comment noting these rows feed tests/e2e/bip-edits.spec.ts and that the bip_edits INSERT requires migration 00017 (applied before test run).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('playwright.config.ts','utf8');if(!/\(admin-review\|bip-edits\)/.test(c))process.exit(1);const s=fs.readFileSync('supabase/seed.e2e.sql','utf8');if(!/bip_edits/.test(s)||!/e2e-edit-target-bip/.test(s))process.exit(1);console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "(admin-review|bip-edits)" playwright.config.ts
    - grep -c "bip-edits" playwright.config.ts == 1 (coordinator-authed line NOT changed)
    - grep -q "bip_edits" supabase/seed.e2e.sql AND grep -q "e2e-edit-target-bip" supabase/seed.e2e.sql
    - grep -q "status='approved'\|status = 'approved'" supabase/seed.e2e.sql in the new block
  </acceptance_criteria>
  <done>admin-authed project matches bip-edits; seed has an approved target BIP + a differing pending bip_edits row, idempotently inserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → cloud DB | Seed runs with service-role at test time; no production data touched |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | seed.e2e.sql reuse of a prod-consumed BIP | mitigate | Dedicated literal uuid + slug 'e2e-edit-target-bip'; never reuse admin-review's "Machine Learning Foundations" |
| T-08-02 | Repudiation | flaky double-run of bip-edits across two projects | accept→mitigate | Assign spec to admin-authed only; spawn coordinator context in-spec (no double-run) |
</threat_model>

<verification>
- `npx playwright test --list tests/e2e/bip-edits.spec.ts` lists 8 tests
- Config + seed grep assertions pass
</verification>

<success_criteria>
Failing harness exists: spec (8 tests, all EDIT grep-names), admin-authed wiring, approved+pending-edit seed.
</success_criteria>

<output>
After completion, create `.planning/phases/08-edit-approved-request-changes/08-01-SUMMARY.md`
</output>
