---
phase: 08-edit-approved-request-changes
plan: 06
type: execute
wave: 3
depends_on: [08-03, 08-04]
files_modified:
  - components/admin/BipEditDiffView.tsx
  - components/admin/AdminBipCard.tsx
  - components/admin/AdminBipRow.tsx
  - app/(admin)/admin/page.tsx
autonomous: true
requirements: [EDIT-03, EDIT-08]
user_setup: []

must_haves:
  truths:
    - "BipEditDiffView renders every editable field side-by-side (live | proposed) with changed fields gold-highlighted and a changed-count header"
    - "The admin queue shows new submissions and edits together; edit items carry an 'Edit' badge"
    - "changes_requested items render the amber badge automatically via STATUS_BADGE_CLASSES"
    - "The queue sub-line reads 'N items awaiting review · includes new submissions and edits' when edits are present"
  artifacts:
    - path: "components/admin/BipEditDiffView.tsx"
      provides: "all-fields side-by-side diff (D-07)"
      min_lines: 80
    - path: "app/(admin)/admin/page.tsx"
      provides: "union queue (submissions + edits) + sub-line copy"
      contains: "getAdminPendingEdits"
  key_links:
    - from: "app/(admin)/admin/page.tsx"
      to: "getAdminPendingSubmissions + getAdminPendingEdits"
      via: "merge + sort by created_at"
      pattern: "getAdminPendingEdits"
    - from: "AdminBipCard 'Edit' badge"
      to: "literal class lookup"
      via: "bg-eu-blue-50 text-eu-blue border-eu-blue-light (no concat)"
      pattern: "bg-eu-blue-50"
---

<objective>
Wave 3 (parallel with 08-05): the admin-facing read surfaces — the all-fields diff view (D-07/EDIT-03) and the unified badged review queue (D-08). The diff view is the only no-analog component in the phase; it derives changed fields purely at render time from the live BIP and the proposed edit.

Purpose: Let an admin see, in one queue, both new submissions and pending edits, and open an edit into a complete side-by-side comparison.
Output: BipEditDiffView + type-badged AdminBipCard/Row + union admin queue page.
</objective>

<execution_context>
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-edit-approved-request-changes/08-UI-SPEC.md
@.planning/phases/08-edit-approved-request-changes/08-PATTERNS.md

<interfaces>
From 08-04: getAdminPendingSubmissions(): AdminBip[]; getAdminPendingEdits(): AdminBipEditItem[] ({id,bip_id,status,created_at,admin_note,bip}); BipEditDetail.data: BipDraftData (lib/queries/bipEdits.ts).
From 08-03: STATUS_BADGE_CLASSES.changes_requested (lib/utils/status.ts).
Live BIP shape for diff = BipDetail (lib/queries/adminBips.ts getAdminBipById).
Existing: components/admin/AdminBipCard.tsx (status pill slot ~line 78), AdminBipRow.tsx (same), app/(admin)/admin/page.tsx (heading + sub-line lines 30-32, getAdminPendingBips usage).

DIFF FIELD LIST (RESOLVED — UI-SPEC rows 10/11 "Virtual start/end date" DROPPED; no such columns in bips). Render ALL editable fields per D-07, in this order (label → BipDraftData key):
  Title→title; Short description→description; Learning outcomes→learning_outcomes; Field of study→isced_f_code; Language of instruction→language_of_instruction; Min. language level→language_level_min; ECTS credits→ects_credits; Max participants→max_participants; Study levels→study_levels (join ', '); Host city→host_city; Physical start date→physical_start_date; Physical end date→physical_end_date; Application deadline→application_deadline; Virtual component→virtual_component_description; Virtual timing→virtual_timing; Green travel→green_travel (Yes/No); Inclusion support→inclusion_support (Yes/No); Eligibility notes→eligibility_notes; How to apply→`${how_to_apply_type}: ${how_to_apply_value}`; Contact name→contact_name; Contact email→contact_email; Partner institutions→partner_universities (newline-separated names).
Slug is NEVER shown (D-10).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create components/admin/BipEditDiffView.tsx</name>
  <read_first>
    - .planning/phases/08-edit-approved-request-changes/08-UI-SPEC.md (Surface 3 — outer card, header, column headers, field row classes desktop/mobile, gold highlight)
    - .planning/phases/08-edit-approved-request-changes/08-RESEARCH.md (Diff View Implementation — JSON.stringify changed-detection)
    - components/admin/AdminActionsPanel.tsx (existing admin card visual conventions)
  </read_first>
  <files>components/admin/BipEditDiffView.tsx</files>
  <action>
'use client'. Props { liveBip: BipDetail; proposedEdit: BipEditDetail }. Build a FIELDS array of { label, key, format? } in the exact order in <interfaces>. For each field compute changed = JSON.stringify(liveValue) !== JSON.stringify(proposedValue). Count changed.

Outer card `rounded-md border border-border bg-white shadow-sm`. Header `px-6 py-4 border-b border-border flex items-center justify-between`: left "Field Comparison" `text-[22px] font-semibold text-ink`; right `{n} field{s} changed` (`text-sm text-muted`), or "No fields changed" (`text-sm text-muted italic`) when 0.

Desktop (>=960px) column header row: `hidden md:grid grid-cols-[180px_1fr_1fr] gap-0 px-6 py-2 bg-bg-soft border-b border-border`; col labels "Field" / green-dot "Live version" / blue-dot "Proposed edit" per UI-SPEC classes.

Field rows desktop: unchanged `grid grid-cols-[180px_1fr_1fr] gap-0 px-6 py-3 border-b border-border hover:bg-bg-soft transition-colors`; changed adds `bg-eu-gold-soft border-l-2 border-l-eu-gold`. Cells: label `text-sm font-semibold text-muted`; live + proposed `text-sm text-ink whitespace-pre-wrap`; null/empty → "—" in `text-muted`.

Mobile (<960px): stacked per field with "Live:" / "Proposed:" prefixes (`text-xs font-semibold text-muted`); changed wrapper `border-l-2 border-l-eu-gold bg-eu-gold-soft px-4 py-3 mb-2 rounded-r`.

Format helpers: booleans→"Yes"/"No"; study_levels→join ', '; partners→newline-joined names; how_to_apply→`${type}: ${value}`. Render values only as text content (never dangerouslySetInnerHTML).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "BipEditDiffView" | awk '{ if ($1==0) print "ok"; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "'use client'" AND grep -q "Field Comparison" components/admin/BipEditDiffView.tsx
    - grep -q "JSON.stringify" components/admin/BipEditDiffView.tsx (change detection)
    - grep -q "bg-eu-gold-soft" AND grep -q "border-l-eu-gold" components/admin/BipEditDiffView.tsx
    - grep -c "virtual_start_date\|virtual_end_date" components/admin/BipEditDiffView.tsx == 0 (dropped non-existent columns)
    - grep -c "slug" components/admin/BipEditDiffView.tsx == 0
    - grep -q "whitespace-pre-wrap" components/admin/BipEditDiffView.tsx
  </acceptance_criteria>
  <done>All-fields side-by-side diff with gold-highlighted changes, changed-count header, desktop grid + mobile stack, no slug, no virtual-date rows.</done>
</task>

<task type="auto">
  <name>Task 2: Type badge on cards + union admin queue</name>
  <read_first>
    - components/admin/AdminBipCard.tsx (status pill slot ~line 78; AdminBip type)
    - components/admin/AdminBipRow.tsx (same slot)
    - app/(admin)/admin/page.tsx (heading + sub-line lines 30-32; current getAdminPendingBips call)
    - lib/queries/bipEdits.ts (getAdminPendingSubmissions, getAdminPendingEdits, AdminBipEditItem)
    - .planning/phases/08-edit-approved-request-changes/08-UI-SPEC.md (Surface 2 — badge classes lookup, sub-line copy)
  </read_first>
  <files>components/admin/AdminBipCard.tsx, components/admin/AdminBipRow.tsx, app/(admin)/admin/page.tsx</files>
  <action>
1) AdminBipCard.tsx + AdminBipRow.tsx: add an optional `kind?: 'submission' | 'edit'` (or `showEditBadge?: boolean`) prop. After the status pill, when kind==='edit' render the "Edit" badge using a LITERAL class string (no concat — Tailwind v4): `inline-flex items-center rounded-full border px-2 py-0.5 text-xs font-semibold bg-eu-blue-50 text-eu-blue border-eu-blue-light`. The changes_requested status badge already renders via STATUS_BADGE_CLASSES (08-03) — no extra JSX. For edit items, the review link href must point to `/admin/bip-edits/${edit.id}/review`; submissions keep `/admin/bips/${bip.id}/review`.

2) app/(admin)/admin/page.tsx: replace the single getAdminPendingBips read with both getAdminPendingSubmissions() and getAdminPendingEdits(); merge into one list tagged by kind and sort by created_at ascending. Render submission cards (no badge, submission review link) and edit cards (Edit badge, edit review link, status from edit row). Compute count = submissions.length + edits.length and hasEdits = edits.length > 0. Sub-line copy (Surface 2): 0 → "You're all caught up. New submissions and edits will appear here automatically."; hasEdits → `${count} items awaiting review · includes new submissions and edits` (use non-breaking spaces ` · `); else → `${count} BIP${count===1?'':'s'} awaiting review`. Heading "Pending review" unchanged.
  </action>
  <verify>
    <automated>grep -q "getAdminPendingEdits" "app/(admin)/admin/page.tsx" && grep -q "bg-eu-blue-50 text-eu-blue border-eu-blue-light" components/admin/AdminBipCard.tsx && grep -q "includes new submissions and edits" "app/(admin)/admin/page.tsx" && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "getAdminPendingSubmissions" AND "getAdminPendingEdits" in app/(admin)/admin/page.tsx
    - grep -q "bg-eu-blue-50 text-eu-blue border-eu-blue-light" in AdminBipCard.tsx AND AdminBipRow.tsx (literal, no `${`)
    - grep -q "/admin/bip-edits/" app/(admin)/admin/page.tsx (edit review link)
    - grep -q "includes new submissions and edits" app/(admin)/admin/page.tsx
    - `npx tsc --noEmit` clean for the three files
  </acceptance_criteria>
  <done>Cards carry the Edit badge + correct review link; queue merges submissions and edits with the mixed-count sub-line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| admin RSC → diff render | Proposed (coordinator-authored) content rendered to admin |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-17 | Information Disclosure | XSS via proposed content in diff | mitigate | React text rendering only; no dangerouslySetInnerHTML; whitespace-pre-wrap formatting |
| T-08-18 | Tampering | dynamic Edit-badge class concatenation | mitigate | Literal class string lookup (Tailwind v4 safelist, Pitfall 13) |
| T-08-19 | IDOR | edit-card review link exposing wrong edit | mitigate | href built from edit.id; review route (08-08) re-gates via getBipEditById role check |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for the four files
- `npx playwright test tests/e2e/bip-edits.spec.ts -g "diff view"` reaches the queue (route completed in 08-08)
</verification>

<success_criteria>
Admin sees a unified badged queue and a complete side-by-side diff (EDIT-03). No slug or non-existent virtual-date rows leak into the diff.
</success_criteria>

<output>
After completion, create `.planning/phases/08-edit-approved-request-changes/08-06-SUMMARY.md`
</output>
