---
phase: 08-edit-approved-request-changes
plan: 08
type: execute
wave: 4
depends_on: [08-05, 08-06]
files_modified:
  - components/admin/RequestChangesBipModal.tsx
  - components/admin/AdminActionsPanel.tsx
  - app/(admin)/admin/bips/[id]/review/page.tsx
  - app/(admin)/admin/bip-edits/[editId]/review/page.tsx
autonomous: true
requirements: [EDIT-03, EDIT-04, EDIT-05, EDIT-06, EDIT-07, EDIT-08]
user_setup: []

must_haves:
  truths:
    - "The admin actions panel shows three buttons (Approve / Request Changes / Reject), labelled for edit vs new submission, wired to the right Server Actions"
    - "RequestChangesBipModal collects a required 10-1000 char note and calls requestChangesEditAction or requestChangesBipAction"
    - "The Request Changes button is enabled only when status==='pending' — matching the action guard (no error-toast-vs-disabled-button mismatch)"
    - "The new /admin/bip-edits/[editId]/review route renders BipEditDiffView + the panel in edit mode"
    - "The existing /admin/bips/[id]/review page gains the Request Changes button for new submissions"
  artifacts:
    - path: "app/(admin)/admin/bip-edits/[editId]/review/page.tsx"
      provides: "edit review route with diff view"
      contains: "BipEditDiffView"
    - path: "components/admin/RequestChangesBipModal.tsx"
      provides: "amber required-note modal"
      contains: "Request Changes"
    - path: "components/admin/AdminActionsPanel.tsx"
      provides: "third button + isEdit/editId props"
      contains: "Request Changes"
  key_links:
    - from: "AdminActionsPanel Approve (isEdit)"
      to: "approveEditAction"
      via: "edit-mode button → action"
      pattern: "approveEditAction"
    - from: "/admin/bip-edits/[editId]/review"
      to: "getBipEditById + getAdminBipById"
      via: "RSC fetch live + proposed for diff"
      pattern: "getBipEditById"
---

<objective>
Wave 4 (parallel with 08-07): the admin verdict surface that turns the E2E green. Add the third "Request Changes" button to the actions panel, the amber required-note modal, the new edit-review route (with the diff view), and the Request-Changes path on the existing new-submission review page (D-06a).

Purpose: Implement EDIT-03 (diff in route), EDIT-04/05/06 verdict UX, wiring EDIT-07/08 through the actions from 08-05.
Output: RequestChangesBipModal + extended AdminActionsPanel + extended bips review page + new bip-edits review page.
</objective>

<execution_context>
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/BipHub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-edit-approved-request-changes/08-UI-SPEC.md
@.planning/phases/08-edit-approved-request-changes/08-PATTERNS.md

<interfaces>
From 08-05: approveEditAction(editId), rejectEditAction(editId,note), requestChangesEditAction(editId,note), requestChangesBipAction(bipId,note). The two request-changes actions both guard pre-image status==='pending'.
From 08-06: BipEditDiffView (liveBip, proposedEdit). From 08-04: getBipEditById(editId): BipEditDetail; getAdminBipById(bipId): BipDetail (existing in adminBips.ts).
From 08-04 schemas: RequestChangesEditSchema, RequestChangesBipSchema.
Analogs: components/admin/RejectBipModal.tsx (Zod+RHF, char counter, disabled-until-valid, error Alert); components/admin/AdminActionsPanel.tsx (props line 20-25, enabled logic 37-39, existing Approve/Reject buttons + modals); app/(admin)/admin/bips/[id]/review/page.tsx (2-column RSC layout).
UI-SPEC Surface 4 enabled logic + helper text; Surface 5 modal spec; Copywriting Contract for all labels/toasts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RequestChangesBipModal + AdminActionsPanel third button</name>
  <read_first>
    - components/admin/RejectBipModal.tsx (full structure to mirror — Zod/RHF, char counter, confirm disabled until valid, error Alert)
    - components/admin/AdminActionsPanel.tsx (props, enabled logic, existing modal wiring)
    - lib/schemas/bip-edits.ts (RequestChangesEditSchema, RequestChangesBipSchema)
    - lib/actions/admin-edit-bips.ts (approveEditAction/rejectEditAction/requestChangesEditAction/requestChangesBipAction — request-changes guards status==='pending')
    - .planning/phases/08-edit-approved-request-changes/08-UI-SPEC.md (Surface 4 + Surface 5)
  </read_first>
  <files>components/admin/RequestChangesBipModal.tsx, components/admin/AdminActionsPanel.tsx</files>
  <action>
1) RequestChangesBipModal.tsx ('use client'): mirror RejectBipModal. Props { open, onOpenChange, bipId, editId?, bipTitle, coordinatorName, isEdit?: boolean }. Use RequestChangesEditSchema when isEdit (defaultValues {editId, note:''}), else RequestChangesBipSchema (defaultValues {bipId, note:''}). On confirm: isEdit → requestChangesEditAction(editId, note); else → requestChangesBipAction(bipId, note). Title "Request Changes"; description "You're about to request changes to:"; gold-border BIP callout `bg-bg-soft border-l-4 border-eu-gold rounded-r px-4 py-3 mb-4`; note label "Note for the coordinator (required)"; Textarea id="request-changes-note" rows=4 maxLength=1000 placeholder per Surface 5; char counter `{n}/1000 characters`; context line "This note will appear on the coordinator's dashboard and in the notification email."; confirm button `bg-status-pending text-white hover:bg-amber-700 rounded-pill px-5 py-2 font-semibold` label "Request Changes" / loading "Requesting…"; disabled when isPending || !form.formState.isValid; success toast "Changes requested. {coordinatorName} will be notified."; server error → Alert variant="destructive".

2) AdminActionsPanel.tsx: extend props with `isEdit?: boolean` and `editId?: string`. Approve label "Approve Edit" when isEdit else "Approve BIP"; Reject label "Reject Edit" / "Reject BIP". Insert the THIRD button between Approve and Reject: `border border-status-pending text-status-pending bg-white hover:bg-status-pending-bg rounded-pill w-full`, MessageSquare icon, "Request Changes", opens RequestChangesBipModal. Enabled logic (WARNING-1 fix — keep the button gate identical to the action guard in 08-05): canApprove = currentStatus==='pending'; canRequestChanges = currentStatus==='pending'; canReject = currentStatus in ('pending','approved') for submissions, currentStatus==='pending' for edits. (Do NOT enable Request Changes on 'changes_requested' — the action guards pending only, so a disabled button is correct here rather than an error toast.) When isEdit, the Approve button calls approveEditAction(editId); Reject opens the edit reject modal calling rejectEditAction(editId,note) (reuse RejectBipModal with an edit-mode prop OR a small edit branch); Request Changes modal gets isEdit + editId. Helper text per Surface 4 (new-submission vs edit-row copy).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "RequestChangesBipModal|AdminActionsPanel" | head -5; grep -q "requestChangesEditAction" components/admin/RequestChangesBipModal.tsx && grep -q "MessageSquare" components/admin/AdminActionsPanel.tsx && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "RequestChangesEditSchema" AND "RequestChangesBipSchema" components/admin/RequestChangesBipModal.tsx
    - grep -q "requestChangesEditAction" AND "requestChangesBipAction" components/admin/RequestChangesBipModal.tsx
    - grep -q "isEdit" AND "editId" components/admin/AdminActionsPanel.tsx
    - grep -q "Request Changes" AND "MessageSquare" components/admin/AdminActionsPanel.tsx
    - canRequestChanges gate is status==='pending' ONLY (grep -Eq "canRequestChanges *= *currentStatus *=== *'pending'" components/admin/AdminActionsPanel.tsx) — must NOT also test 'changes_requested'
    - grep -q "approveEditAction" components/admin/AdminActionsPanel.tsx (edit-mode approve)
    - `npx tsc --noEmit` clean for both files
  </acceptance_criteria>
  <done>Three-button panel (edit + submission labels) + amber required-note modal; Request Changes enabled exactly when status==='pending', matching the 08-05 action guard.</done>
</task>

<task type="auto">
  <name>Task 2: New edit-review route + Request Changes on the submission review page</name>
  <read_first>
    - app/(admin)/admin/bips/[id]/review/page.tsx (2-column RSC layout to copy + the page to extend)
    - components/admin/BipEditDiffView.tsx (08-06)
    - components/admin/AdminActionsPanel.tsx (Task 1 — isEdit/editId)
    - lib/queries/bipEdits.ts (getBipEditById) + lib/queries/adminBips.ts (getAdminBipById)
    - .planning/phases/08-edit-approved-request-changes/08-PATTERNS.md (bip-edits review page section — exact layout)
  </read_first>
  <files>app/(admin)/admin/bip-edits/[editId]/review/page.tsx, app/(admin)/admin/bips/[id]/review/page.tsx</files>
  <action>
1) Create app/(admin)/admin/bip-edits/[editId]/review/page.tsx (`export const dynamic = 'force-dynamic'`). RSC: const { editId } = await props.params; fetch editRow = getBipEditById(editId) (notFound() if null), then bip = getAdminBipById(editRow.bip_id) + coordinator. Two-column layout copied from the bips review page: breadcrumb "← Back to queue" → /admin; left column renders `<BipEditDiffView liveBip={bip} proposedEdit={editRow} />` (above BipHeader/BipBody); right column sticky `<AdminActionsPanel bipId={bip.id} bipTitle={bip.title} coordinatorName={coordinatorName} currentStatus={editRow.status} nextPendingId={null} isEdit={true} editId={editRow.id} />`.

2) app/(admin)/admin/bips/[id]/review/page.tsx: this handles NEW submissions (incl. now status==='changes_requested'). Ensure AdminActionsPanel is rendered WITHOUT isEdit (defaults to submission mode) so the new third "Request Changes" button appears and calls requestChangesBipAction(bipId,note). No diff view here. Confirm the page still resolves for status==='changes_requested' (the union queue now links such items here).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "bip-edits/\[editId\]/review|bips/\[id\]/review" | head -5; grep -q "BipEditDiffView" "app/(admin)/admin/bip-edits/[editId]/review/page.tsx" && grep -q "isEdit={true}" "app/(admin)/admin/bip-edits/[editId]/review/page.tsx" && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - app/(admin)/admin/bip-edits/[editId]/review/page.tsx exists; grep -q "getBipEditById" AND "BipEditDiffView" AND "isEdit={true}" AND "editId="
    - grep -q "force-dynamic" app/(admin)/admin/bip-edits/[editId]/review/page.tsx
    - the bips review page renders AdminActionsPanel in submission mode (no isEdit prop) → Request Changes button available
    - `npx tsc --noEmit` clean for both files
  </acceptance_criteria>
  <done>Edit items open into a diff + edit-mode panel; new submissions gain the Request Changes verdict on their existing review page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| admin browser → verdict actions | Verdict buttons invoke privileged Server Actions |
| admin review route → edit content | Another coordinator's proposed edit shown to admin |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-23 | IDOR | forged editId in /admin/bip-edits/[editId]/review | mitigate | getBipEditById gates role==='admin' and runs under (admin) group; notFound() on null |
| T-08-24 | Input Validation | empty/oversized request-changes note | mitigate | RequestChanges*Schema min 10 / max 1000 client + server safeParse (08-05) |
| T-08-25 | Spoofing | createAdminClient misuse | mitigate | Review pages use server queries (anon+JWT) under app/(admin); no createAdminClient introduced |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for the four files
- `npm run build && npx playwright test tests/e2e/bip-edits.spec.ts` — all 8 tests green
</verification>

<success_criteria>
Admin can view a diff (EDIT-03), approve/reject/request-changes edits and request changes on new submissions (EDIT-04/05/06) with emails (EDIT-07) and audit rows (EDIT-08); the Request Changes button gate matches the action guard. bip-edits.spec.ts is green.
</success_criteria>

<output>
After completion, create `.planning/phases/08-edit-approved-request-changes/08-08-SUMMARY.md`
</output>
